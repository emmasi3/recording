# 八、43、项目为什么 disable 拷贝、只保留移动？

---

## 一、问题

> **项目为什么 disable 拷贝、只保留移动？**

这是第 43 问的原题。考察候选人对 `FrameWrapper` / `PacketWrapper` 底层资源管理机制的理解，以及为什么选择 move-only 设计而非利用 FFmpeg 引用计数 API。

---

## 二、用户原始回答

### （1）禁用拷贝的核心原因

> 项目中，对于 FrameWrapper、PacketWrapper 都禁用了拷贝构造，因为他们托管的资源是 AVFrame、AVPacket，这些资源都依赖引用计数管理，如果只是简单的进行浅拷贝，那么未来在释放的时候，可能会造成 double-free（二次释放），程序会报错，所以只对他们进行移动构造或者移动操作，更安全。

### （2）FFmpeg 引用计数 API 的存在

> ffmpeg 关于 AVFrame、AVPacket 的引用计数操作提供了专门的接口，用户可以依靠如：`av_packet_ref`···方法，封装专用的方法，对数据包的引用计数进行合理的操作，这样也可以保证拷贝的正确性，当然，这些功能需要用户自己去实现。

---

## 三、标准参考答案

### 3.1 直接原因：防止 double-free

| 类 | 底层资源 | 析构行为 | 若浅拷贝会发生什么 |
|----|----------|----------|---------------------|
| `FrameWrapper` | 裸指针 `AVFrame*` | 析构时调用 `av_frame_free(&frm_)` | 两个实例指向同一块内存，各自 `free` → **double-free 崩溃** |
| `PacketWrapper` | `std::unique_ptr<AVPacket, AVPacketDeleter>` | `unique_ptr` 析构自动调 `av_packet_free` | 两个 `unique_ptr` 共享同一指针 → **double-free 崩溃** |

因此，项目将拷贝构造和拷贝赋值 **显式 `= delete`**，仅保留移动语义。

### 3.2 设计选择：移动语义传递独占所有权

```cpp
// FrameWrapper — 移动构造：资源所有权转移，源对象置空
FrameWrapper(FrameWrapper&& other) noexcept 
    : frm_(other.frm_), owned_(other.owned_) {
    other.frm_ = nullptr;
    other.owned_ = false;
}

// PacketWrapper — 移动构造：unique_ptr 天然支持移动，default 即可
PacketWrapper(PacketWrapper&&) = default;
```

在整个数据管线中，帧/包从采集流向编码再流向推流，方向是**单向的**，不存在"多处同时持有同一份数据"的需求，因此 `move-only` 架构完全足够，且避免了引用计数的开销。

### 3.3 关于 FFmpeg 引用计数 API（加分说明）

FFmpeg 内部的 `AVBufferRef` 机制天然支持引用计数：
- `av_frame_ref(dst, src)` → 为 data buffer 创建新的引用计数副本，多个 `AVFrame` 安全共享同一块数据缓冲区
- `av_packet_ref(dst, src)` → 同理，安全的"引用计数拷贝"

但项目中的 `FrameWrapper` / `PacketWrapper` **并没有使用这些 API**，而是走**独占所有权**路线。原因在于：
- 数据管线中包/帧单向流动，无需多实例共享
- move-only 避免了引用计数的原子操作开销
- 所有权清晰，调试和维护更简单

**如果需要实现"拷贝"语义**，可以基于 `av_frame_ref` / `av_packet_ref` 自行封装引用计数拷贝方法，这也是正确的做法——但项目当前的设计不需要。

---

## 四、用户回答评分与改进建议

### 4.1 评分分析

| 要点 | 用户回答 | 评分 | 说明 |
|------|----------|------|------|
| 防止 double-free | ✅ 已覆盖 | ★★★★ | 核心原因讲清楚了，方向正确 |
| 资源所有权设计 | ✅ 已覆盖 | ★★★ | 提到了"只保留移动"，但未展开移动语义的实现方式 |
| 浅拷贝危险 | ✅ 已覆盖 | ★★★★ | 明确了浅拷贝 → 二次释放的逻辑链路 |
| 表述笔误 | ❌ | ☆☆ | "FrameWrapper、FrameWrapper"重复写错类名，应为"FrameWrapper、PacketWrapper" |
| "资源依赖引用计数管理" | ⚠️ 表述有歧义 | ★★☆ | AVFrame/AVPacket 内部虽然有引用计数，但项目并未利用它，说是"依赖引用计数管理"不够准确 |
| FFmpeg ref API 补充 | ✅ 加分项 | ★★★★ | 主动提出 `av_packet_ref` 等替代方案，展现了对 FFmpeg 底层的了解 |
| 项目为什么不用 ref API | ❌ 缺失 | ☆☆☆ | 未解释为何项目选择 move-only 而非用 ref API 做安全拷贝 |

### 4.2 改进建议

| 编号 | 问题 | 改进方向 |
|------|------|----------|
| ① | **笔误**：两次写了 FrameWrapper | 修正为 "FrameWrapper 和 PacketWrapper" |
| ② | **"资源依赖引用计数管理"** 表述不准确 | 改为："底层资源 AVFrame/AVPacket 是独占资源，如果浅拷贝会导致两个实例指向同一块内存，析构时各自释放 → double-free" |
| ③ | **未形成逻辑层次** | 三点应分层递进：直接原因（防 double-free）→ 设计选择（move-only 管线）→ 备选方案（FFmpeg ref API） |
| ④ | **未引用项目代码** | 可顺带提及 `FrameWrapper` 的 `owned_` 标志和移动构造实现，增强说服力 |
| ⑤ | **未解释为何不用 ref API** | 应说明管线单向流动 + 避免原子操作开销 = move-only 更合适 |

---

## 五、模拟面试完整回答（参考）

> **第一，直接原因是防止 double-free。** `FrameWrapper` 持有裸指针 `AVFrame*`，`PacketWrapper` 持有 `unique_ptr<AVPacket, AVPacketDeleter>`。如果允许默认的浅拷贝，两个实例会指向同一块 FFmpeg 内存，析构时各自调用 `av_frame_free` 或 `av_packet_free`，造成 double-free，程序直接崩溃。所以我们把拷贝构造和拷贝赋值显式 `= delete`，只保留移动构造和移动赋值。
>
> **第二，从设计角度，管线中数据是单向流动的。** 采集线程拿到一帧 → 放入队列 → 编码线程取出 → 编码后推流。整个过程中每一帧在同一时刻只有一个持有者，不存在多个地方需要同时共享同一帧的场景。所以 move-only 足以满足需求，还避免了引用计数的原子操作开销，所有权也一目了然。
>
> **第三，FFmpeg 确实提供了引用计数 API 来做安全拷贝。** 例如 `av_frame_ref(dst, src)` 会让两个 `AVFrame` 共享内部 `AVBufferRef`，由 FFmpeg 内部来管理引用计数，析构时不会 double-free。如果项目将来需要多处共享同一帧（比如预览 + 推流 + 录制三条管线），可以基于这些 API 封装引用计数拷贝方法。但目前的设计不需要，move-only 已经足够简洁高效。

---

## 六、核心要点速记（对照复习用）

| 编号 | 要点 | 一句话总结 |
|------|------|------------|
| ① | 禁用拷贝 | `FrameWrapper` / `PacketWrapper` 拷贝构造和拷贝赋值均 `= delete` |
| ② | double-free | 浅拷贝 → 两个实例指向同一 FFmpeg 资源 → 析构时各自 free → 崩溃 |
| ③ | 独占所有权 | `FrameWrapper` 用裸指针 + `owned_` 标记，`PacketWrapper` 用 `unique_ptr` |
| ④ | 移动语义 | 移动构造/赋值转移所有权，源对象指针置空，保证资源自始至终只有一个持有者 |
| ⑤ | 管线架构 | 采集 → 编码 → 推流，数据单向流动，无需多实例共享 |
| ⑥ | FFmpeg ref API | `av_frame_ref` / `av_packet_ref` 可通过引用计数实现安全拷贝，但项目未采用 |
| ⑦ | 何时用 ref API | 当数据需要"一对多"共享时（预览+推流+录制），可基于 ref API 封装引用计数拷贝 |
| ⑧ | 与题 44 的关系 | `AVPacketDeleter` 自定义删除器配合 `unique_ptr` 实现 RAII，与本问的 move-only 设计互为补充 |

---

## 七、项目源码关键代码速查

### FrameWrapper（裸指针 + owned_ 标志）

```cpp
// FrameWrapper.h — 禁用拷贝，保留移动
FrameWrapper(const FrameWrapper&) = delete;
FrameWrapper& operator=(const FrameWrapper&) = delete;

// 移动构造：资源转移，源对象置空
FrameWrapper(FrameWrapper&& other) noexcept 
    : frm_(other.frm_), owned_(other.owned_) {
    other.frm_ = nullptr;
    other.owned_ = false;
}
```

### PacketWrapper（unique_ptr + 自定义删除器）

```cpp
// PacketWrapper.h — 自定义删除器
struct AVPacketDeleter {
    void operator()(AVPacket* pkt) const { av_packet_free(&pkt); }
};

// PacketWrapper — unique_ptr 天然禁止拷贝，支持移动
class PacketWrapper {
    using PacketPtr = std::unique_ptr<AVPacket, AVPacketDeleter>;
    PacketPtr m_pkt;
public:
    PacketWrapper(const PacketWrapper&) = delete;
    PacketWrapper& operator=(const PacketWrapper&) = delete;
    PacketWrapper(PacketWrapper&&) = default;
};
```
