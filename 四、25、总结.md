# 四、25、输出流 `time_base` 的自动赋值与项目处理 — 总结

---

## 一、问题

**25. 输出流 `time_base` 何时被 FFmpeg 自动赋值？项目中怎么处理的？**

参见面试题文档：
> 这是项目文档中记录的一个重要发现：**在 `avformat_write_header()` 调用之后**，`outStream->time_base` 才会被正确初始化（通常为 `{1, 90000}`）。在此之前该字段可能是 `{0, 0}`。
>
> 项目中的解决方案：在 `Open()` 的最后（`avformat_write_header` 之后）才通过 `set_vOut_time_base` / `set_aOut_time_base` 回传给编码器，并在 MuxThreadProc 中通过检查 `(v_tb.den + v_tb.num) == 0` 来判断是否为纯音频/纯视频模式。项目文档中也建议：不要假设 time_base 会提前初始化，应始终在使用前检查。

---

## 二、用户原始回答

### （1）`time_base` 何时被赋值

> 输出流中的 `time_base` 在调用 `avformat_write_header` 时，自动被 FFmpeg 赋值；在此之前，该字段值为 `{0, 1}` 分数形式。

### （2）项目中怎么处理的

> 在调用 `avformat_write_header` 后，拿到了输出流对应的 `time_base` 字段，之后在写入输出流时，用于音视频编码包时间戳有关字段的缩放，以 `time_base` 为目标时间基。

---

## 三、标准参考回答

### 3.1 `time_base` 何时被赋值

`avformat_write_header()` 是输出流 `time_base` 被正式赋值的**分界线**：

| 阶段 | `outStream->time_base` 状态 | 说明 |
|------|---------------------------|------|
| **调用前** | `{0, 1}` 或 `{0, 0}`（无效占位值） | FFmpeg muxer 尚未初始化该字段，不可使用 |
| **调用后** | 由封装格式自动决定 | FLV → `{1, 1000}`，MP4 → `{1, 90000}` 等 |

---

### 3.2 项目中的处理流程

整个流程分为**三步**，核心思路是"先等 FFmpeg 赋值 → 再回传编码器 → 最后统一缩放"：

#### 第一步：`avformat_write_header` 后立即回传时间基

在 `FfmpegMuxerStreamer::Open()` 末尾，写头完成后立即从输出流取出正确的 `time_base`，回传给编码器：

```cpp
// 7) 头写完后，修正音视频编码器输出 time_base
if (a_outStream && m_aEncoder)
{
    auto audio_encoder_ptr = std::dynamic_pointer_cast<AudioFfmpegEncoder>(m_aEncoder);
    if (audio_encoder_ptr)
    {
        audio_encoder_ptr->set_aOut_time_base(a_outStream->time_base);
    }
}
if (v_outStream && m_vEncoder)
{
    auto video_encoder_ptr = std::dynamic_pointer_cast<VideoFfmpegEncoder>(m_vEncoder);
    if (video_encoder_ptr)
    {
        video_encoder_ptr->set_vOut_time_base(v_outStream->time_base);
    }
}
```

#### 第二步：编码器保存输出时间基

编码器内部用成员变量 `m_out_time_base` / `m_aOut_time_base` 保存这个值。

#### 第三步：每次编码出包时统一缩放

编码后的 `AVPacket` 通过 `av_packet_rescale_ts` 从编码器内部时间基 → 输出流时间基：

- **视频**：`av_packet_rescale_ts(m_pkt, m_ctx->time_base, m_out_time_base)`
  - 从 `{1, fps}`（编码器）缩放至 `{1, 90000}`（MP4）或 `{1, 1000}`（FLV）
- **音频**：`av_packet_rescale_ts(m_pkt, m_ctx->time_base, m_aOut_time_base)`
  - 从 `{1, sample_rate}`（编码器）缩放至输出流要求的时间基

---

### 3.3 关键代码链路图

```
avformat_write_header()                  ← FFmpeg 自动赋值 outStream->time_base
         │
         ▼
set_vOut_time_base(outStream->time_base) ← 回传给视频编码器
set_aOut_time_base(outStream->time_base) ← 回传给音频编码器
         │
         ▼
avcodec_receive_packet() → 获得编码包
         │
         ▼
av_packet_rescale_ts(pkt, src_tb, dst_tb) ← 用保存的输出时间基做目标缩放
         │
         ▼
av_interleaved_write_frame()             ← 写入正确时间戳的包
```

---

### 3.4 追问预判：如果忘记这一步会怎样？

> 如果不在 `avformat_write_header` 之后才取 `time_base`，`m_out_time_base` 将始终保持占位值 `{0, 1}`。
>
> `av_packet_rescale_ts` 内部需要除以 `dst_tb.den`（即 0），会触发**浮点异常**或导致 **PTS 全部为 0/无效值**，播放器无法正常解析时间线，Nginx RTMP 端也可能因此拒绝连接。

---

### 3.5 项目中判断纯音频/纯视频模式的技巧

在 MuxThreadProc 中还有一个与 `time_base` 相关的实用技巧——通过检查时间基是否为零来判断流是否存在：

```cpp
bool isVideoOnly = (a_tb.den + a_tb.num) == 0;  // 纯视频模式
bool isAudioOnly = (v_tb.den + v_tb.num) == 0;  // 纯音频模式
```

因为 `avformat_write_header` 后，不存在的流其 time_base 仍为 `{0, 0}`。

---

## 四、核心要点速记

| 要点 | 一句话总结 |
|------|----------|
| **赋值时机** | `avformat_write_header()` 调用后，由 muxer 根据封装格式自动填充 |
| **调用前值** | 占位值 `{0, 1}` 或 `{0, 0}`，不可使用 |
| **项目策略** | 写头后立即回传 → 编码器保存 → 每包出时统一缩放 |
| **缩放 API** | `av_packet_rescale_ts(pkt, 编码器tb, 输出流tb)` |
| **常见后果** | 忘记处理 → PTS 全部无效 → 播放器/服务器异常 |
