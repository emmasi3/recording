# 八、44、AVPacketDeleter 自定义删除器的设计意图

---

## 一、问题

> **`AVPacketDeleter` 自定义删除器是什么？设计意图是什么？**

此题考察对 **FFmpeg 资源管理与 C++ RAII 结合** 的理解，属于资源生命周期管理类面试题。

---

## 二、用户原始回答

| 编号 | 用户回答要点 | 简述 |
|------|------------|------|
| （1） | 原本 `AVPacket` 并非 RAII 机制，内存管理不自动；用 `unique_ptr<AVPacket>` + 自定义删除器实现 RAII，方向可行。 | RAII 转化意图 |
| （2） | 需注意两点：① 删除器逻辑要合理，符合 FFmpeg 的 `AVPacket` 正确释放流程，避免内存泄漏；② 用智能指针接管对象时，应**立即清理原有对象或直接移动**，保证独享语义。 | 删除器正确性 + 移动语义 |

### 用户回答的优点
- **方向正确**：抓住了"非 RAII → RAII"这条主线。
- **安全意识到位**：提到了避免内存泄漏和保证独占语义。

### 用户回答的不足
- 未点名删除器内部调用的具体 FFmpeg API（`av_packet_free` vs `av_packet_unref`）。
- 未解释为何选择 `unique_ptr` 而非 `shared_ptr`。
- "立即清理原有对象"表述略显模糊，实际代码中使用的是 `std::move`。
- 未与第 43 问（禁用拷贝、只保留移动）形成逻辑串联。

---

## 三、标准参考回答

### 3.1 核心回答框架（推荐面试表述）

> **第一，设计意图：把非 RAII 的 `AVPacket` 包装成 RAII 资源。**
>
> `AVPacket` 在 FFmpeg 中需要手动调用 `av_packet_free` 释放，容易遗漏造成内存泄漏。我们定义 `AVPacketDeleter` 结构体，其 `operator()` 内部调用 `av_packet_free(&pkt)` ——注意这里是传 `AVPacket**`，它同时做 **`av_packet_unref`**（减少内部 data buffer 引用计数）和 **`av_free`**（释放结构体本身），是**完全释放**。
>
> **第二，和 `unique_ptr` 配合实现独占所有权 + 自动释放。**
>
> `using PacketPtr = std::unique_ptr<AVPacket, AVPacketDeleter>`，`unique_ptr` 析构时自动调用删除器，开发者无需手动管理。同时 `unique_ptr` 天然禁止拷贝、只允许移动，与第 43 问 `PacketWrapper` 禁用拷贝的 move-only 设计完全一致——管线中数据单向流动，不需要多实例共享同一包。
>
> **第三，在构造函数中通过 `std::move` 传递所有权。**
>
> `PacketWrapper(PacketPtr pkt) : m_pkt(std::move(pkt)) {}` 将外部 `unique_ptr` 所有权转移到成员 `m_pkt`，源指针自动置空，保证独享语义不被打乱。
>
> **第四，区分"完全释放"和"仅 unref"两种场景。**
>
> `AVPacketDeleter` 走 `av_packet_free` 做完全释放；`Pakcet_unref()` 方法走 `av_packet_unref` 只清空内部数据引用，保留 `AVPacket` 结构体供复用。

### 3.2 关键 API 对比

| 函数 | 作用 | 调用位置 |
|------|------|----------|
| `av_packet_free(&pkt)` | **完全释放**：`av_packet_unref` + `av_free` | `AVPacketDeleter::operator()`（unique_ptr 析构时自动调） |
| `av_packet_unref(pkt)` | **仅清空引用**：减少内部 data buffer 引用计数，不清结构体本身 | `PacketWrapper::Pakcet_unref()`（手动调用，复用场景） |

### 3.3 项目中关键代码

```cpp
// AVPacketDeleter 的定义（PacketWrapper.h）
struct AVPacketDeleter {
    void operator()(AVPacket* pkt) const noexcept {
        av_packet_free(&pkt);  // 完全释放：unref + free 结构体
    }
};

// PacketPtr 类型别名
using PacketPtr = std::unique_ptr<AVPacket, AVPacketDeleter>;

// move-only 构造函数（PacketWrapper.cpp）
PacketWrapper::PacketWrapper(PacketPtr pkt) :
    m_pkt(std::move(pkt))  // 所有权转移，源指针自动置空
{}

// 手动 unref 方法（仅清空数据引用）
void PacketWrapper::Pakcet_unref() {
    if (m_pkt.get())
        av_packet_unref(m_pkt.get());
}
```

### 3.4 为什么用 `unique_ptr` 而非 `shared_ptr`

| 考量 | 说明 |
|------|------|
| 数据流向 | 采集 → 编码 → 推流，**单向流动**，任意时刻只有一个持有者 |
| 语义匹配 | `unique_ptr` 禁止拷贝、只允许移动，天然匹配管线模式 |
| 一致性 | 与第 43 问 `= delete` 拷贝构造的设计一脉相承 |
| 性能 | 无需维护引用计数的原子操作开销 |

---

## 四、评估打分

| 维度 | 用户得分 | 满分 | 说明 |
|------|:--:|:--:|------|
| 核心意图（RAII 转化） | 4 | 5 | 方向正确 |
| FFmpeg 释放流程细节（`av_packet_free` vs `av_packet_unref`） | 1 | 3 | 未展开具体 API |
| 与 `unique_ptr` 配合的设计考量（独占/移动/防拷贝） | 2 | 3 | 提到但不够精确 |
| 与第 43 问的逻辑串联 | 0 | 2 | 未提及 |
| 代码实现细节（move 构造、Pakcet_unref） | 1 | 2 | 描述模糊 |
| **总分** | **8** | **15** | — |

---

## 五、面试避坑指南

1. **不要只说"自动释放"就停** —— 一定要点出调的是 `av_packet_free` 而非 `av_packet_unref`，并解释两者的区别。
2. **不要把 `unique_ptr` 和 `shared_ptr` 搞混** —— 这里用 `unique_ptr` 是有设计考量的，面试官很可能追问"为什么不用 `shared_ptr`"。
3. **主动串联前问** —— 第 43 问（禁止拷贝）+ 第 44 问（RAII 删除器）本质讲的是同一套资源管理体系，串联回答能展现系统性思维。
4. **"立即清理原有对象"这种表述建议换成 `std::move`** —— 更精确，也暗示你熟悉 C++11 移动语义。
