# 26. `av_interleaved_write_frame` 和 `av_write_frame` 有什么区别？你用的是哪个？

---

## 一、问题

> `av_interleaved_write_frame` 和 `av_write_frame` 有什么区别？你用的是哪个？

**考察点**：FFmpeg 封装（muxing）过程中两种写入 API 的核心差异、各自适用场景、以及在实际项目中的选型理由。

---

## 二、用户原始回答

1. `av_interleaved_write_frame` 处理 AVPacket 时，会按照该包的 **时间戳进行自动排序**，视频包、音频包交错存放，用户只需填充正确时间戳即可；写入时还会进行检查，如 **音视频包的 PTS < DTS 时会直接报错**，从而终止程序。

2. `av_write_frame` 处理 AVPacket 时，**直接写入文件，不做自动排序**，写入顺序决定包在媒体文件中的位置；自由度更高、更灵活，但用户需自行负责排序；且**没有对应的 PTS、DTS 检查机制**。

3. **我选择 `av_interleaved_write_frame`**：与 `av_write_frame` 相比性能差距不大，但功能和便捷性远高于后者；当前项目无高灵活度需求场景，即使未来需要也可方便替换。

---

## 三、标准参考回答

### 3.1 核心区别（纠错 + 准确表述）

| 维度 | `av_interleaved_write_frame` | `av_write_frame` |
|---|---|---|
| **排序** | 内部维护交错缓冲队列，按各流 packet 的 **DTS（而非 PTS）** 自动排序后写入 | 不做任何排序，调用者传什么顺序就按什么顺序写入 |
| **校验** | 校验**同一流内 DTS 是否单调递增**，DTS 回退时报错 `AVERROR(EINVAL)` | 只做基本合法性校验（如 stream_index），**不走交错排序，也不做 DTS 单调性校验** |
| **适用场景** | RTMP 推流、FLV/MP4 等需要音视频严格交错的容器 | 高自由度自定义写入顺序的场景 |
| **性能** | 多了一个内部队列入队+排序开销，但顺序到达时几乎不触发重排，开销可忽略 | 无额外开销 |

### 3.2 关键纠错点（用户回答中的错误）

| 用户原话 | 错误原因 | 正确表述 |
|---|---|---|
| "按 PTS 自动排序" | B 帧场景下 PTS ≠ DTS，解码器按 DTS 消费，必须按 **DTS** 排序 | 排序依据是 **DTS（解码时间戳）** |
| "PTS < DTS 时会直接报错" | B 帧的 PTS 本就可能大于 DTS，这不是错误；真正的校验是 **DTS 回退（非单调递增）** | 检查的是**同一流内 DTS 是否单调递增**，不一致则报错 |
| "av_write_frame 没有检查机制" | 表述过于绝对，av_write_frame 也有基本校验 | 它**不做交错排序和 DTS 单调性约束**，但仍有 stream_index 等基本校验 |

### 3.3 为什么排序用的是 DTS 而不是 PTS？

- **DTS（Decoding Time Stamp）**：决定"什么时候解码"，数据喂给解码器的顺序必须按 DTS 递增。
- **PTS（Presentation Time Stamp）**：决定"什么时候显示"。
- 存在 B 帧时，编码顺序 ≠ 显示顺序（如 I-P-B 结构中，B 帧先解码但后显示），按 PTS 排序写入会导致解码失败。
- 因此 FFmpeg 的 muxer 内部按 **DTS** 排序，确保解码器能线性消费。

### 3.4 实际项目选型理由

项目中使用 `av_interleaved_write_frame`（见 `FfmpegMuxerStreamer.cpp:247` 和 `RtmpMuxerStreamer.cpp`），理由如下：

1. **RTMP 推流要求严格交错**：协议层面要求音视频包按 DTS 交替排列，手动控制极易出错。
2. **编码器产出天然 DTS 有序**：屏幕录制场景下，编码器输出的包已经是 DTS 递增的，`av_interleaved_write_frame` 的内部缓冲队列几乎无需真正重排，性能开销忽略不计。
3. **工程安全性更高**：自动排序 + DTS 单调性校验 = 避免因时间戳异常导致容器损坏或推流中断。
4. **可替换性**：如有特殊场景（如自定义包序写入），可通过条件编译或策略模式快速切到 `av_write_frame`，不存在锁定风险。

---

## 四、进阶：代码中的实际使用

```cpp
// FfmpegMuxerStreamer.cpp:247
bool LocalMuxer::WritePacket(AVPacket* packet) {
    int ret = av_interleaved_write_frame(m_ctx, packet);
    if (ret < 0 && ret != AVERROR(EAGAIN)) {
        char errbuf[AV_ERROR_MAX_STRING_SIZE];
        av_strerror(ret, errbuf, sizeof(errbuf));
        LOG_ERROR(g_logger) << "av_interleaved_write_frame failed: " << errbuf;
        return false;
    }
    // ...
}
```

- 对 `AVERROR(EAGAIN)` 做了特殊处理（非阻塞模式下的重试语义），说明项目考虑了推流临时阻塞的情况。
- 两个 muxer（本地文件 / RTMP 推流）统一使用 `av_interleaved_write_frame`，保持接口一致性。

---

## 五、面试答题建议（简洁版）

> **（1）核心区别：**
> - `av_interleaved_write_frame`：内部维护交错缓冲队列，按各流 packet 的 **DTS** 自动排序后写入。同时校验同一流内 DTS 是否单调递增，回退则报错。RTMP/FLV/MP4 场景下几乎是必须的。
> - `av_write_frame`：不做任何排序，调用者传什么顺序就写什么顺序，完全由用户保证 DTS 递增和音视频交错。灵活但容错性低。
>
> **（2）性能对比：**
> 多了一次内部队列的入队+排序开销，但在屏幕录制实时编码场景下，包已天然 DTS 有序，几乎不需重排，性能差异可忽略。
>
> **（3）我的选择理由：**
> - 屏幕录制 + RTMP 推流要求音视频严格交错
> - 编码器产出包已 DTS 有序，无需手动控制写入顺序
> - 性能开销可忽略，工程安全性更高
> - 如有特殊需求可快速替换，不存在技术锁定
