# 面试题 28：单路流（纯视频/纯音频）处理 &"意图路由"机制

---

## 一、原题

> **28. 如果只有视频/只有音频，怎么处理？`LocalMuxer::Open()` 中"意图路由"是什么？**

---

## 二、用户原始回答

> **（1）** 如果输入流只有 video/audio 时，直接在 `LocalMuxer::Open()` 创建输出上下文时，就仅创建某一路目标流，并且在 `MuxThreadProc` 这个封装线程中，通过检查视频、音频的编码器上下文中时间基字段是否被初始化来判断完成标志的填充，方便快速判断音视频是否需要同步（一路流的话，就不需要同步了）；
>
> **（2）** 意图路有的话，就是在创建输出上下文后，通过传入的媒体文件名称 `m_filename` 带后缀的，来判断是否只需要音频、视频、音视频，从而在上下文中创建对应的媒体流；本质来说，意图路由 —— 就是判断该上下文的音视频需要的标志；

---

## 三、标准参考回答

### 3.1 单路流在 `MuxThreadProc` 中的处理

#### 3.1.1 判断机制：利用 time_base 反向检测

`avformat_write_header()` 之后，如果某路流未被创建（即 `avformat_new_stream` 未调用），则对应的 `AVStream` 指针为 `nullptr`，就不会向编码器回传该流的 `time_base`，导致该路的 `time_base` 保持初始值 `{0, 0}`：

```cpp
// MuxThreadProc 中获取输出流时间基
AVRational v_tb = vEncPtr ? vEncPtr->get_vOut_time_base() : AVRational{ 0, 0 };
AVRational a_tb = aEncPtr ? aEncPtr->get_aOut_time_base() : AVRational{ 0, 0 };

// 利用 time_base 是否为 {0,0} 判断哪路流不存在
if ((v_tb.den + v_tb.num) == 0)    // → is_only_audio = true
if ((a_tb.den + a_tb.num) == 0)    // → is_only_video = true
```

#### 3.1.2 同步修正：绕过 `av_compare_ts`

单路流时，音视频同步没有意义。通过强制覆盖 `cmp` 实现"跳过同步"：

```cpp
// 修正 cmp -- 根据 is_only_video/audio
if (is_only_video)
    cmp = -1;          // 强制走视频分支（cmp <= 0）
else if (is_only_audio)
    cmp = 1;           // 强制走音频分支（cmp > 0）
```

> **注意**：`av_compare_ts` 仍会执行计算，只是结果被覆盖。并非"不做同步"，而是"绕过同步结果，强制走对应分支"。

#### 3.1.3 Done 退出逻辑中也复用了该标志

```cpp
// 结束标记：单路流时对应的 queue_empty 条件自动满足
if ((video_queue_empty || is_only_audio) 
    && (audio_queue_empty || is_only_video))
    break;
```

### 3.2 `LocalMuxer::Open()` 中的"意图路由"

"意图路由"是一个**双层决策模型**，决定输出上下文是否需要创建视频流 / 音频流。

#### 3.2.1 第一层：文件后缀 → 用户意图

```cpp
const bool forceAudioOnly =
    (ext == "aac" || ext == "mp3" || ext == "wav" || ext == "flac" || ext == "opus" || ext == "m4a");
const bool forceVideoOnly =
    (ext == "h264" || ext == "264" || ext == "h265" || ext == "265" || ext == "hevc");
```

| 后缀类型 | 支持的格式 | `forceAudioOnly` | `forceVideoOnly` |
|---------|-----------|:---:|:---:|
| 纯音频 | `.aac` `.mp3` `.wav` `.flac` `.opus` `.m4a` | `true` | `false` |
| 纯视频 | `.h264` `.264` `.h265` `.265` `.hevc` | `false` | `true` |
| 其他 | `.mp4` `.flv` `.mkv` 等 | `false` | `false` |

#### 3.2.2 第二层：封装格式能力检查

```cpp
const bool ofmtHasVideo = (m_ctx->oformat->video_codec != AV_CODEC_ID_NONE);
const bool ofmtHasAudio = (m_ctx->oformat->audio_codec != AV_CODEC_ID_NONE);
```

| 封装格式 | `ofmtHasVideo` | `ofmtHasAudio` |
|---------|:---:|:---:|
| `.mp4` | ✅ | ✅ |
| `.flv` | ✅ | ✅ |
| `.mkv` | ✅ | ✅ |
| `.h264` 裸流 | ✅ | ❌ |
| `.aac` 裸流 | ❌ | ✅ |

#### 3.2.3 最终决策：能力 ∩ 意图

```cpp
bool needVideo = ofmtHasVideo && !forceAudioOnly;
bool needAudio = ofmtHasAudio && !forceVideoOnly;
```

| 场景 | 计算过程 | `needVideo` | `needAudio` |
|------|---------|:---:|:---:|
| 录制到 `.mp4` | `true && true` / `true && true` | ✅ | ✅ |
| 录制到 `.aac` | `false && false` / `true && true` | ❌ | ✅ |
| 录制到 `.h264` | `true && true` / `false && false` | ✅ | ❌ |
| 录制到 `.flv`（视频流） | `true && true` / `true && true` | ✅ | ✅ |

> **核心原则**：格式要先**有能力**承载该媒体流（`ofmtHasXxx`），用户再**表达**想要该媒体流（`!forceXxxOnly`），两者取**逻辑与（AND）**——一个"能力 ∩ 意图"的决策模型。

#### 3.2.4 意图路由的结果落地

```cpp
// needVideo 决定是否创建视频流
if (needVideo) {
    v_outStream = avformat_new_stream(m_ctx, nullptr);   // 创建流
    m_vEncoder = VideoFfmpegEncoder::createNew(...);      // 创建编码器
    m_vIndex = v_outStream->index;                         // 记录流索引
} else {
    m_vIndex = -1;                                         // 标记不存在
}

// needAudio 同理
if (needAudio) { ... m_aIndex = a_outStream->index; }
else { m_aIndex = -1; }
```

---

## 四、回答对比与改进建议

| 维度 | 用户回答 | 标准回答 | 差距分析 |
|------|----------|----------|----------|
| 单路流 - Open 阶段 | 提到"仅创建某一路目标流" | 明确指出 `needVideo`/`needAudio` → `avformat_new_stream` 按需创建 | 大体正确，未点出关键变量 |
| 单路流 - MuxThreadProc 检测 | "检查编码器上下文中时间基字段是否被初始化" | 实际检查的是**输出流 time_base**（来自 `AVStream->time_base`），非 `AVCodecContext->time_base` | 表述不够精确，容易产生歧义 |
| 单路流 - 同步处理 | "不需要同步" | 补充说明 `av_compare_ts` 仍然执行，结果被强制覆盖，是"绕过"而非"不做" | 理解正确，表述可更严谨 |
| 意图路由 - 后缀判断 | "通过文件名后缀来判断" | 指出了具体的后缀列表（`.aac`/`.h264` 等 11 种格式） | 缺少具体枚举 |
| 意图路由 - 双层决策 | 未提及 | 明确了"格式能力 ∩ 用户意图"的双层 AND 模型 | 遗漏关键设计思想 |
| Done 退出逻辑 | 未提及 | `is_only_audio/video` 也用于简化退出条件判断 | 遗漏 |
| time_base 检查的潜在风险 | 未提及 | 若 time_base 被意外赋值非零值，`(den+num)==0` 将失效 | — |

### 面试官可能追问的点

1. **"用 `(v_tb.den + v_tb.num) == 0` 判断流不存在有什么隐患？"**
   → 这是一种"利用副作用"的隐式判断。如果某处代码意外给 time_base 赋了非零值但流实际不存在，判断会失效。更稳健的做法是直接用 `m_vIndex == -1` / `m_aIndex == -1` 判断。

2. **"如果用户传入 `.mp4` 但只想录音频，怎么实现？"**
   → 当前设计通过后缀判断，`.mp4` 会同时创建音视频流。只能通过修改后缀（如 `.aac`）来指定纯音频。如果需要更灵活的接口，可以额外提供 `bool videoOnly / audioOnly` 参数。

3. **"封装格式不支持但用户强行走，会怎样？"**
   → 代码中有防御：`if (!needVideo && !needAudio) { return false; }`，会直接失败并打印错误日志。

---

## 五、完整流程图

```
用户传入文件名 (m_filename)
         │
         ▼
  ┌─────────────────────┐
  │ 1. 提取后缀 (ext)    │
  │    判断 forceXxxOnly │
  └────────┬────────────┘
           │
           ▼
  ┌─────────────────────────┐
  │ 2. 检查封装格式能力       │
  │    ofmtHasVideo / Audio  │
  └────────┬────────────────┘
           │
           ▼
  ┌──────────────────────────────┐
  │ 3. 双层决策 (能力 ∩ 意图)      │
  │    needVideo = ofmt && !force │
  │    needAudio = ofmt && !force │
  └────────┬─────────────────────┘
           │
           ▼
  ┌───────────────────────┐
  │ 4. 按需创建流           │
  │    needVideo? → new stream
  │    needAudio? → new stream
  │    否则 → m_xIndex = -1 │
  └────────┬──────────────┘
           │
           ▼
  ┌──────────────────────────┐
  │ 5. write_header           │
  │    → time_base 回传编码器  │
  └────────┬─────────────────┘
           │
           ▼
  ┌──────────────────────────────┐
  │ 6. MuxThreadProc 主循环       │
  │    检测 time_base → is_only   │
  │    强制覆盖 cmp → 跳过同步    │
  └──────────────────────────────┘
```

---

## 六、关键代码索引

| 功能 | 文件 | 行号范围 |
|------|------|---------|
| 后缀意图路由（`forceXxxOnly`） | `src/adapters/output/FfmpegMuxerStreamer.cpp` | ~80-83 |
| 封装格式能力检查 | 同上 | ~89-90 |
| 双层决策（`needVideo/Audio`） | 同上 | ~98-99 |
| 按需创建视频流 | 同上 | ~108-142 |
| 按需创建音频流 | 同上 | ~144-175 |
| MuxThreadProc - 获取 time_base | 同上 | ~382-383 |
| MuxThreadProc - time_base 检测单路流 | 同上 | ~385-393 |
| MuxThreadProc - 强制修正 cmp | 同上 | ~465-472 |
| MuxThreadProc - done 退出逻辑复用 | 同上 | ~448-453 |
