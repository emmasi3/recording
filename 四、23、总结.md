# 四、23、总结 — VideoEncoder 与 AudioEncoder 对 AVERROR(EAGAIN) 的处理差异

---

## 一、问题原文

> **第 23 问**：VideoEncoder 和 AudioEncoder 遇到 `AVERROR(EAGAIN)` 时分别如何处理？两者为什么有不同的处理逻辑？

---

## 二、用户原始回答

### (1) VideoEncoder

> `videoEncoder`，遇到 `AVERROR(EAGAIN)` 时，直接退出循环 → 退出本次编码，等待下次调用，不会报错；或者在另一个重载函数中，直接返回编码包队列。

### (2) AudioEncoder

> `audioEncoder`，遇到 `AVERROR(EAGAIN)` 时，立即调用一次 `avcodec_receive_packet` 接收一帧编码包，调用用户回调函数处理，然后再次调用 `avcodec_send_frame` 尝试，直到返回 0 即发送成功为止；当然，对于 audio 也有返回编码包数组的重载，和视频的处理方式一样，只不过本项目没有采用返回编码包数组的方式。

---

## 三、用户回答评价

| 评估维度 | 评分 | 说明 |
|---------|:---:|------|
| Video 重载一（返回数组）| ✅ 正确 | "EAGAIN 时直接返回编码包队列"准确 |
| Video 重载二（回调模式）| ✅ 正确 | "退出循环等待下次调用，不报错"准确 |
| Audio 发送失败重试机制 | ✅ 正确 | "先 receive 排空 → 再 retry send"完全正确 |
| Audio 数组重载未使用 | ✅ 正确 | 确认数组重载存在但本项目仅打 warn 不做实际编码 |
| 两者差异的原因 | ❌ 未提及 | 未解释为什么视频不重试而音频要重试（NVENC 大缓冲 vs AAC 小帧） |
| 设计思想 | ❌ 未提及 | 未提及"消费者优先于生产者"的 EAGAIN 处理哲学 |
| 总体 | **75-85%** | 流程描述完全正确，缺少架构层面的 why 分析 |

---

## 四、标准参考回答

### 4.1 VideoEncoder（视频编码器）—"遇 EAGAIN 即停"

视频编码器有两个重载，处理 EAGAIN 的策略一致：**不重试，直接退出**。

**重载一：`Encode(frame)` → `vector<PacketWrapperPtr>`**（第 90-129 行）

```
avcodec_send_frame → 若失败直接 return {}
avcodec_receive_packet 循环:
  ├── 拿到包 → 加入 vector
  ├── 遇到 EOF 或 EAGAIN → return std::move(vec)  ✅ 立即返回
  └── 错误 → 记录日志，跳过该包
```

**重载二：`Encode(frame, callback)` → `int`**（第 131-185 行）

```
avcodec_send_frame → 若失败 return ret
avcodec_receive_packet 循环:
  ├── 遇到 EOF 或 EAGAIN → break  ✅ 退出循环，返回 0
  └── 拿到包 → 回调 func(pkt)
```

> **关键特点**：两种重载在 EAGAIN 时都**不报错、不重试**，直接结束本次编码。原因见 4.3。

### 4.2 AudioEncoder（音频编码器）—"遇 EAGAIN 先排空再重试"

音频编码器在 `avcodec_send_frame` 返回 EAGAIN 时，有一套**主动排空 + 重试**机制（第 478-517 行）：

```
do {
    ret = avcodec_send_frame(m_ctx, aframe);
    if (ret == AVERROR(EAGAIN)) {
        // ① 立即调用 avcodec_receive_packet 排空一帧
        avcodec_receive_packet(m_ctx, m_pkt);
        
        // ② 设置流信息 + 时间戳
        m_pkt->stream_index = m_aOutIndex;
        av_packet_rescale_ts(m_pkt, ...);
        
        // ③ 回调 func(m_pkt) 处理编码包
        func(m_pkt);
        
        // ④ 释放引用计数
        av_packet_unref(m_pkt);
        
        // ⑤ 继续 while 循环，再次尝试 avcodec_send_frame
    }
} while (ret < 0);  // 直到 send_frame 成功（ret >= 0）才退出
```

> **关键特点**：遇到 EAGAIN → 立即 `receive_packet` 排空 → 回调处理 → 释放 → 再次 `send_frame`，**循环直到发送成功**。

音频也有返回数组的重载（第 462-467 行），但仅打印 warn 并返回空数组，**本项目不使用**。

### 4.3 为什么两者处理逻辑不同？

| 对比维度 | 视频编码器 (NVENC) | 音频编码器 (libfdk_aac / AAC) |
|---------|-------------------|------------------------------|
| **内部缓冲大小** | 非常大（NVENC 独立 ASIC 芯片，硬件缓冲充足） | 非常小（软件编码器，帧小缓冲有限） |
| **EAGAIN 触发概率** | 极低（实际运行中几乎不出现） | 较高（AAC 编码器内部队列容易积压） |
| **处理策略** | 遇 EAGAIN 则停止，等待下次调用 | 遇 EAGAIN 则主动排空 + 重试，确保不丢帧 |
| **设计思想** | "让子弹飞一会儿"——等待编码器自行消化 | "消费者优先于生产者"——先取走已有的，腾出空间再生产 |
| **如果反过来会怎样** | 不重试也没关系，因为下次调用时缓冲已排空 | 不重试 → 本次音频帧被丢弃 → 可能会出现卡顿/无声 |

### 4.4 数据流对比

```
【视频编码器 — EAGAIN 处理】
avcodec_send_frame → OK
avcodec_receive_packet → EAGAIN → break/return ✅
                                （编码器还在处理，下次再来取）

【音频编码器 — EAGAIN 处理】  
avcodec_send_frame → EAGAIN ❌（编码器内部队列满了！）
    │
    ├─ avcodec_receive_packet() → 取出一帧编码包
    ├─ func(pkt) 回调处理
    ├─ av_packet_unref(pkt) 释放
    │
    └─ 再次 avcodec_send_frame → 成功 ✅
       （队列腾出空间，可以写入了）
```

---

## 五、源码证据

```cpp
// src/adapters/codec/FfmpegEncoder.cpp

// ========== 视频编码器 - 重载一（第 90-129 行）==========
std::vector<PacketWrapperPtr> VideoFfmpegEncoder::Encode(const FramePtr& frame) 
{
    ret = avcodec_send_frame(m_ctx, frame->Buffer()->Get());
    if (ret < 0) return {};  // send 失败直接返回空，不重试

    while (true) {
        ret = avcodec_receive_packet(m_ctx, pkt);
        if (ret == AVERROR_EOF || ret == AVERROR(EAGAIN))
            return std::move(vec);  // ✅ EAGAIN → 直接返回已收集的包
        // ... 收集到 vec
    }
}

// ========== 视频编码器 - 重载二（第 154-183 行）==========
int VideoFfmpegEncoder::Encode(const FramePtr& frame, callback) 
{
    // send_frame 同上...
    do {
        ret = avcodec_receive_packet(m_ctx, m_pkt);
        if (ret == AVERROR_EOF || ret == AVERROR(EAGAIN))
            break;  // ✅ EAGAIN → 退出循环，不报错
        // ... func(m_pkt)
    } while (true);
    return 0;
}

// ========== 音频编码器（第 478-517 行）==========
int AudioFfmpegEncoder::Encode(const FramePtr& frame, callback) 
{
    do {
        ret = avcodec_send_frame(m_ctx, aframe);
        if (ret < 0) {
            if (ret == AVERROR(EAGAIN)) {
                // ✅ EAGAIN → 先排空再重试
                ret_1 = avcodec_receive_packet(m_ctx, m_pkt);
                // ... 设置流信息、时间戳
                m_pkt->stream_index = m_aOutIndex;
                av_packet_rescale_ts(m_pkt, ...);
                func(m_pkt);          // 回调处理
                av_packet_unref(m_pkt);// 释放
                // 继续 while 循环，再次 avcodec_send_frame
            }
            // ... 其他错误处理
        }
    } while (ret < 0);  // ← 直到发送成功！
}

// ========== 音频编码器 - 数组重载（第 462-467 行）==========
std::vector<PacketWrapperPtr> AudioFfmpegEncoder::Encode(const FramePtr& frame)
{
    LOG_WARN(g_logger) << "... is unuseful, please use callback version";
    return std::vector<PacketWrapperPtr>();  // 空实现
}
```

---

## 六、面试通关总结

### 核心记忆框架

```
"视频 EAGAIN → 直接停（buffer 大，等得起）
 音频 EAGAIN → 先排空再重试（buffer 小，不能丢）
 本质：生产者-消费者协调，音频更激进，视频更保守。"
```

### 一句话口诀

> **"视频遇满直接走，音频遇满排空再投。"**

### 可能的追问

| 追问 | 参考答案 |
|------|---------|
| 为什么不给视频也加重试？ | NVENC 内部缓冲极大，EAGAIN 几乎不出现；视频帧数据量大（1080p ~6MB/帧），强行重试可能导致更大的延迟和帧堆积 |
| 音频重试死循环怎么办？ | `do-while` 中有 `AVERROR_EOF` 和 `AVERROR(EAGAIN)` 的双重保护（receive 那步检查），不会死循环 |
| 音频为什么不直接用数组重载？ | 为了低延迟——回调模式可以边编码边推流，不用等所有包都收完再一次性推出去 |

---

*整理日期：2026-07-07*
