# 三、17、`swr_convert` 音频重采样参数 — 总结

---

## 一、问题

**17. `swr_convert` 重采样你都设置了哪些参数？为什么需要重采样？**

参见面试题文档：
> 输入参数来自音频解码器上下文（麦克风原始格式），输出参数来自 AAC 编码器上下文。通过 `swr_alloc_set_opts2` 设置：
> - 采样率：如 48000 → 44100
> - 采样格式：如 `AV_SAMPLE_FMT_S16` → `AV_SAMPLE_FMT_FLTP`
> - 声道布局：保持一致（立体声）
>
> 项目中 `SwrContext` 只创建一次，不需要在运行时重建（设备参数不变）。

---

## 二、用户原始回答

### （1）关于设置了哪些参数

> 在音频重采样上下文中，设置了目标(期望)音频属性：采样格式、采样率、声道布局。

### （2）关于"为什么需要重采样"

> 重采样是为了让原本的音频属性满足接受者的期望，在这里是符合编码器要求的音频属性，当然，这些用户都是可以自己设定程序进行协商的。

---

## 三、标准参考回答

### 3.1 设置的参数（三对映射）

重采样的**输入参数**来自音频**解码器上下文**（DShow 麦克风原生格式），**输出参数**来自 AAC **编码器上下文**（硬性要求）。通过 `swr_alloc_set_opts2` 一次性设置三组映射：

| 参数类别 | 输入（来源：解码器） | 输出（来源：编码器） | 说明 |
|---------|-------------------|-------------------|------|
| **采样率** | 如 48000 Hz（麦克风原生） | 如 44100 Hz（编码器要求） | 不同设备采样率不同 |
| **采样格式** | 如 `AV_SAMPLE_FMT_S16`（16位整型交错） | `AV_SAMPLE_FMT_FLTP`（32位浮点 planar） | AAC 编码器硬性要求 FLTP |
| **声道布局** | 立体声 | 立体声（保持一致） | 声道一般不需改变 |

核心 API 调用链：`swr_alloc_set_opts2` → `swr_init` → `swr_convert`。

### 3.2 实际代码（项目中的初始化）

```cpp
bool DshowAudioCapture::init_swrCtx()
{
    AVChannelLayout in_ch_layout  = m_decoder->getCtx()->ch_layout;   // 输入：解码器
    AVChannelLayout out_ch_layout = m_encoder->getCtx()->ch_layout;   // 输出：编码器
    swr_alloc_set_opts2(
        &m_swrCtx,
        &out_ch_layout,
        m_encoder->getCtx()->sample_fmt,   // 输出采样格式
        m_encoder->getCtx()->sample_rate,  // 输出采样率
        &in_ch_layout,
        m_decoder->getCtx()->sample_fmt,   // 输入采样格式
        m_decoder->getCtx()->sample_rate,  // 输入采样率
        0, nullptr
    );
    if (swr_init(m_swrCtx) < 0) { ... }
    return true;
}
```

**注意**：`swr_alloc_set_opts2` 的参数顺序是 **先输出，后输入**（out_ch_layout → out_sample_fmt → out_sample_rate → in_ch_layout → in_sample_fmt → in_sample_rate），容易记混。

### 3.3 为什么需要重采样？

核心原因：**麦克风原生输出格式 ≠ AAC 编码器输入格式**，二者不匹配，需要重采样做"翻译"：

1. **采样格式不匹配**：DShow 麦克风通常输出 `AV_SAMPLE_FMT_S16`（交错存储），而 AAC 编码器要求 `AV_SAMPLE_FMT_FLTP`（planar 浮点格式，每个声道独立存储）。这是编码器的**硬性约束**，不满足则直接报错，没有协商余地。

2. **采样率不匹配**：不同麦克风设备采样率可能不同（如 44100/48000 Hz），编码器预设的采样率也可能不同。需要统一转换。

3. **帧大小要求**：AAC 编码器要求固定的 `frame_size`（通常 1024 采样点/帧），而麦克风吐出的帧大小不固定（可能是 1002、1044 等），需要借助 `AVAudioFifo`（采样点级队列）配合重采样来做"凑帧"。

### 3.4 `SwrContext` 生命周期

项目中 `SwrContext` **只创建一次**（在 `Open()` 的初始化流程中调用 `init_swrCtx()`），设备参数在运行时不变，因此不需要重建，避免重复分配带来的性能开销。

---

## 四、用户回答 vs 标准回答 对比分析

| 维度 | 用户回答 | 标准回答 | 评价 |
|------|---------|---------|:----:|
| **设置的参数** | "设置了采样格式、采样率、声道布局" | 三组映射：输入参数来自解码器、输出参数来自编码器 | ✅ 三个方向说对了 |
| **输入/输出区分** | 只提了"目标(期望)属性" | 明确区分输入来自解码器上下文、输出来自编码器上下文 | ⚠️ 只说了一半，面试官可能追问"源属性从哪来" |
| **具体 API 名称** | 未提及 | `swr_alloc_set_opts2` → `swr_init` → `swr_convert` | ❌ 缺失——面试中说出关键 API 名称能证明你真的写过代码 |
| **采样格式具体值** | 未提及 | `S16` → `FLTP`（整型交错 → 浮点 planar） | ❌ 缺少具体数值，回答显得空洞 |
| **采样率具体值** | 未提及 | `48000` → `44100` | ❌ 缺少数值例子 |
| **声道布局** | 提到"声道布局" | "保持一致（立体声）" | ✅ 方向对，但未说明输入输出一致 |
| **SwrContext 生命周期** | 未提及 | 创建一次，不重建（设备参数不变） | ❌ 缺失 |
| **为什么需要重采样** | "满足编码器要求的属性" | AAC 编码器硬性要求 FLTP 格式、固定 frame_size，不存在"协商" | ⚠️ 用词"协商"不准确——编码器格式是硬性约束 |
| **`swr_get_delay`** | 完全未提及 | 动态缓冲区计算的关键 API | ❌ 高光加分点缺失 |

**综合评估**：大方向没错，但缺乏具体细节和深度。面试官大概率会追问："输入参数从哪来的？""为什么不直接用固定缓冲区？""有没有考虑过延迟累积问题？"

---

## 五、关键 API 速查表

| 步骤 | 关键 API | 说明 |
|------|---------|------|
| 创建重采样上下文 | `swr_alloc_set_opts2(&ctx, out_ch, out_fmt, out_rate, in_ch, in_fmt, in_rate, 0, nullptr)` | 先输出后输入，参数易记混 |
| 初始化重采样器 | `swr_init(m_swrCtx)` | 校验参数合法性并完成内部初始化 |
| 查内部缓存延迟 | `swr_get_delay(m_swrCtx, base_rate)` | 返回重采样器内部未输出的采样点数 |
| 计算输出缓冲大小 | `av_rescale_rnd(swr_get_delay + frame->nb_samples, enc_rate, dec_rate, AV_ROUND_UP)` | 根据输入/输出采样率比率精确推算 |
| 执行重采样 | `swr_convert(m_swrCtx, out_data, out_samples, in_data, in_samples)` | 返回值 = 实际输出的采样点数 |
| 释放上下文 | `swr_free(&m_swrCtx)` | 析构时调用 |

---

## 六、深度追问：`swr_get_delay` 的作用与不使用的后果

### 6.1 什么是 `swr_get_delay`？

```cpp
int swr_get_delay(SwrContext *s, int64_t base);
```

返回重采样器**内部缓冲区中尚未输出的采样点数**（以 `base` 指定的采样率为单位）。

### 6.2 为什么重采样器有内部缓存？

重采样不是简单的"输入 N → 输出 M"一一映射。以 44100Hz → 48000Hz 为例，比率是 **48000/44100 ≈ 1.088...** 的非整数。`swr_convert` 内部的多相滤波器需要一定数量的历史样本做插值，且由于比率不是整数，每次调用后总有一些"不够凑成完整输出样本"的残余留在内部缓冲区。

### 6.3 项目中的正确做法（动态策略）

```cpp
// 根据当前内部缓存 + 输入帧的采样数，精确计算本次期望的输出采样数
dstNbSamples = av_rescale_rnd(
    swr_get_delay(m_swrCtx, dec_sample_rate) + frame->nb_samples,
    enc_sample_rate,
    dec_sample_rate,
    AV_ROUND_UP
);

// 如果输出缓冲不够大，则重新分配
if (dstNbSamples > maxNbSamples) {
    av_frame_free(&newFrame);
    newFrame = AllocAudioFrame(dstNbSamples);
}
```

### 6.4 如果始终使用固定缓冲区（不调用 `swr_get_delay`），会发生什么？

#### 阶段 1：初始无延迟，侥幸正常

| 迭代 | 输入样本 | 内部延迟 | 期望输出 | 固定缓冲 | 结果 |
|------|---------|---------|---------|---------|------|
| 1 | 1002 | 0 | ~1024 | 1024 | ✅ 正常 |
| 2 | 1044 | ~2 | ~1064 | 1064 | ✅ 正常（但内部已残留微量样本） |

#### 阶段 2：延迟悄然累积

| 迭代 | 输入 | 内部延迟 | 期望输出 | 固定缓冲 | 实际输出 | 新残留 |
|------|-----|---------|---------|---------|---------|-------|
| 3 | 1002 | 2 | ~1026 | **1024** ❌ | 1024 | ~4 |
| 4 | 1044 | 4 | ~1066 | **1064** ❌ | 1064 | ~6 |
| 5 | 1002 | 6 | ~1030 | **1024** ❌ | 1024 | ~12 |

每次"装不下"的样本永久留在重采样器内部，`swr_get_delay` 持续增长。

#### 阶段 3：延迟失控

```
录制 10 分钟 → 内部缓存积压 0.5~2 秒音频
录制 1 小时  → 内部缓存积压 5~10 秒音频
```

#### 四大灾难性后果

| 后果 | 说明 |
|------|------|
| **音频延迟线性增长** | 对着麦克风说话，10 秒后才在推流/录制中听到——实时推流场景完全不可用 |
| **输出帧 PTS 系统性偏差** | 编码器每帧仍是 1024 采样点，但这些采样点对应的**实际采集时刻已发生偏移**，PTS 与真实时间脱节 |
| **录制结束时的"音频海啸"** | Flush 阶段，重采样器积压的几千个采样点一次性吐出，文件尾部出现"音频比视频多好几秒"的诡异 AV 不同步 |
| **潜在缓冲区溢出** | 延迟持续增长到 FFmpeg 内部上限后，可能返回错误或行为未定义 |

### 6.5 对比总结

| 维度 | 固定缓冲区（不更新） | 动态更新（当前做法） |
|------|--------------------|--------------------|
| 延迟 | 线性累积，无限增长 | 始终 ≤ 1 帧的抖动量 |
| nb_samples | 不可预测，偏差累积 | 精确可控 |
| 内存 | 内部缓冲区持续膨胀 | 外部缓冲区按需扩缩，总量可控 |
| 长时录制 | AV 逐渐不同步 | 稳定同步 |
| Flush 尾部 | "音频海啸"，大量残留帧 | 正常结束，至多几帧残留 |
| 实时推流 | 不可用 | 可用 |

### 6.6 一句话总结

> 不调用 `swr_get_delay` 来动态更新输出缓冲区大小，等于**假装重采样器没有内部状态**。由于采样率比率的非整数特性，每次 `swr_convert` 都会留下微量残余，这些残余像"滚雪球"一样持续累积，最终导致音频延迟线性增长、AV 同步崩溃，对实时推流场景是**致命的**。

---

## 七、优化版完整回答（供背诵参考）

> **问题：`swr_convert` 重采样你都设置了哪些参数？为什么需要重采样？**
>
> 重采样的输入参数来自音频**解码器上下文**（麦克风原生格式），输出参数来自 AAC **编码器上下文**。通过 `swr_alloc_set_opts2` 一次性设置三组映射：
>
> | | 输入（解码器） | 输出（编码器） |
> |---|-------------|-------------|
> | **采样率** | 如 48000 Hz | → 44100 Hz |
> | **采样格式** | `AV_SAMPLE_FMT_S16` | → `AV_SAMPLE_FMT_FLTP` |
> | **声道布局** | 立体声 | → 立体声（一致） |
>
> 为什么需要重采样？因为 DShow 麦克风原生输出格式和 AAC 编码器的输入格式通常不匹配——AAC 编码器对采样格式有硬性要求（必须是 FLTP planar 格式），且编码器要求固定帧长（1024 采样点），必须经过重采样才能将 PCM 数据转换为编码器可接受的格式。
>
> 项目中 `SwrContext` 只创建一次（`init_swrCtx` 在 `Open()` 中调用），设备参数不变，不需要运行时重建。
>
> 另外还有一个关键细节：每次调用 `swr_convert` 前，需要用 `swr_get_delay` 获取重采样器内部缓存中未输出的采样数，结合当前帧的 `nb_samples`，用 `av_rescale_rnd` 动态计算本次应该输出的采样数。如果不这样做，采样率比率的非整数特性会导致残余样本像滚雪球一样累积，最终造成音频延迟线性增长、AV 音视频同步崩溃。

---

## 八、面试要点 Checklist

- [ ] 能说出三对参数映射：采样率（输入→输出）、采样格式（如 S16 → FLTP）、声道布局
- [ ] 能明确区分**输入参数来自解码器上下文**（麦克风原生），**输出参数来自编码器上下文**（AAC 要求）
- [ ] 能说出关键 API 名称：`swr_alloc_set_opts2` → `swr_init` → `swr_convert`
- [ ] 能解释为什么需要重采样：采样格式硬性约束（S16 ≠ FLTP）、采样率差异、固定帧长要求
- [ ] 能说明 `SwrContext` 生命周期：只创建一次，不重建
- [ ] 能解释 `swr_get_delay` 的作用：获取内部缓存未输出样本数，用于动态调整输出缓冲区
- [ ] 能说明不调用 `swr_get_delay` 的严重后果：延迟滚雪球、AV 同步崩溃、"音频海啸"
- [ ] 能写出动态缓冲区计算公式：`av_rescale_rnd(swr_get_delay + frame->nb_samples, enc_rate, dec_rate, AV_ROUND_UP)`
- [ ] 能区分"硬性约束"与"可协商"：AAC 编码器的 FLTP 格式是硬性约束，不是用户可以协商的

---

*本文档整合自用户回答与面试标准参考答案，供复习对照使用。*
