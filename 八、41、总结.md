# 八、41、总结 — Singleton 对 QPC 和 SDL 的用法优缺点

---

## 一、面试问题

> **41. 项目中的 `Singleton` 对 `QPC` 和 `SDL` 的用法，优缺点？**

---

## 二、用户原始回答

### （1）QPC 部分

- QPC 是一个参考时钟，底层用 Windows 平台的高精度计数器（`QueryPerformanceCounter`）实现，方便获取某一时刻的时间戳（本项目关注于视频时间戳）。
- 想在处理采集视频时保持一定的频率，就需要这样一个工具。
- **优点**：利用全局单例，随时随地可以调用，生命周期为整个程序运行期间，对 QPC 正好适用。
- **缺点**：如果不同模块都需要自己的时间戳，每个模块要定制特定功能，全局只有一个单例就不够用，无法满足这样的需求。

### （2）SDL 部分

- `SDL_event_thread` 是全局唯一的实例，用于监听键盘等用户操作事件，并同时管理线程的生命周期。
- 全局唯一是一个不错的设计思路，该模块在当前设计下不需要另外的实例。
- **优点**：方便用户使用。
- **缺点**：不易扩展，想在单例的基础上添加更多事务，代价偏高。

---

## 三、标准/参考回答

> **优点**：全局唯一，任意位置获取实例方便。
>
> **缺点**：隐式耦合、单元测试不可 mock、多线程初始化时序不确定、销毁顺序不可控。
>
> 更好的方式是全部通过 `AppContext` 传递，目前代码中这两处单例是向旧架构的妥协。

---

## 四、详细评测与深入分析

### 4.1 QPC 单例深度剖析

#### 关键源码结构

| 方法 | 语义 | 关键行为 |
|------|------|----------|
| `NowUs()` | 返回**绝对时间戳**（微秒） | 基于 `QueryPerformanceCounter` 计算，调用频率无关 |
| `NowMs(bool is_update)` | 返回**增量/差值**（毫秒） | 计算与上次基准 `PreviousTs` 的差；`is_update=true` 时更新基准点 |

**关键发现**：`NowMs()` 不是"获取当前时刻"，而是"获取与上次 mark 点的间隔"——它是一个带状态的增量计算。

实际调用场景（帧率控制）：
```cpp
// DxgiScreenCapture::isPass()
int64_t delta_time = QPC::GetInstance()->NowMs(false);  // 不更新，只读增量
if (delta_time >= (1000 * 1.0 / m_fps)) {
    // 间隔够了，送入帧
    QPC::GetInstance()->NowMs();  // is_update=true，重置基准点
    return true;
}
```

#### 用户回答对比

| 维度 | 用户回答 | 深度评价 |
|------|----------|----------|
| 基本语义 | ✅ 正确理解 QPC 用途 | 方向准确 |
| 时间戳语义 | ⚠️ 表述"获得某一时刻时间戳"偏泛化 | 未区分 `NowUs`(绝对) vs `NowMs`(增量) |
| 核心缺点 | ⚠️ 只说"不同模块定制不够"，停留在功能层面 | 遗漏耦合、可测试性、生命周期安全三个关键点 |

#### 完整缺点清单

| 序号 | 缺点 | 解释 |
|------|------|------|
| 1 | **隐式耦合** | 任意模块写 `QPC::GetInstance()` 就绑死了具体实现，想替换时间源（如测试用模拟时钟）必须改代码 |
| 2 | **不可测试** | 单元测试无法 mock 时间戳，无法伪造"某一时刻"来验证帧率控制逻辑 |
| 3 | **全局隐藏状态** | `static inline int64_t PreviousTs` 是全局变量，所有调用者共享同一个基准点，语义不透明、容易出错 |
| 4 | **销毁顺序不可控** | 静态局部变量在程序退出时按构造逆序析构，若其他静态对象析构时还调用 QPC，可能出问题 |

#### 项目中的更优方案

项目已有 `ITimestampProvider` 接口 + `SteadyTimestampProvider` + `PassthroughClockSync`，通过依赖注入传递时间源，QPC 单例只是向旧代码的妥协。

---

### 4.2 SDL 单例深度剖析

#### 关键设计缺陷

**缺陷一：`Start()` 阻塞调用线程（致命）**

```cpp
void SDL_event_Thread::Start() {
    // ...启动所有管理线程...
    SDL_Window* window = SDL_CreateWindow(...);
    SDL_Event event;
    do {
        SDL_WaitEvent(&event);  // ← 阻塞等待事件
        switch (event.type) {
            case SDL_QUIT: Stop(); break;
            case SDL_KEYDOWN:
                if (event.key.keysym.sym == SDLK_q) Stop(); break;
        }
    } while (m_state != STATE::Term);  // ← 死循环，直到用户按 Q 退出
    // ...
}
```

调用 `Start()` 的线程会被**永久阻塞**在事件循环里，后续代码无法执行。

**缺陷二：`Stop()` 无超时 join，潜在死锁**

```cpp
void SDL_event_Thread::Stop() {
    m_state = STATE::Term;
    for (auto& t : m_threads) {
        if (t.joinable()) {
            t.join();  // ← 无超时等待，工作线程若 block 则永久卡死
        }
    }
}
```

**缺陷三：功能耦合，违反单一职责原则**

事件监听（SDL 事件循环）+ 线程生命周期管理（`Start` / `Stop` / `join`）揉在一个类里，任一方面要扩展都很痛苦。

#### 用户回答对比

| 维度 | 用户回答 | 深度评价 |
|------|----------|----------|
| 基本语义 | ✅ 正确，全局唯一实例管理事件和线程 | 理解准确 |
| 阻塞行为 | ❌ 未提及 | `Start()` 阻塞是整个设计最关键的痛点 |
| join 无超时 | ❌ 未提及 | 潜在死锁风险 |
| 功能耦合 | ⚠️ 说"不易扩展"，但表述泛化 | 未指出具体违反了什么设计原则 |

---

## 五、改进后的完整回答

### QPC 单例

**优点**：全局唯一，`QPC::GetInstance()` 一行代码即可获取高精度时间，生命周期贯穿整个程序运行期间。

**缺点（设计层面）**：

1. **隐式耦合** — 任何模块写 `QPC::GetInstance()` 就绑死了这个具体实现，想替换时间源（比如测试用模拟时钟）改不了代码。
2. **不可测试** — 单元测试无法 mock 时间戳，无法伪造"某一时刻"来验证帧率控制逻辑。
3. **全局共享状态** — `NowMs()` 依赖 `static inline int64_t PreviousTs` 全局隐藏变量，不同调用者共享同一个基准点，语义不透明。
4. **项目已有更优方案** — `ITimestampProvider` 接口 + 依赖注入（`SteadyTimestampProvider` + `PassthroughClockSync`），QPC 单例只是向旧架构的妥协。

### SDL 单例

**优点**：全局唯一实例，集中管理键盘事件和所有工作线程的生命周期，调用方只关心"注册线程函数"这一件事。

**缺点（设计层面）**：

1. **`Start()` 阻塞调用线程（最致命）** — 内部 `do { SDL_WaitEvent() } while` 死循环，调用后主线程再也出不来，程序结构被"锁死"。
2. **`Stop()` 无超时 join** — 遍历 `m_threads` 逐个 join，没有任何超时机制，工作线程如果 block 就永久死锁。
3. **功能耦合，违反单一职责** — 事件监听 + 线程管理揉在一个单例里，任一方面要扩展都很痛苦。
4. **同样有测试困难、销毁顺序问题** — 与 QPC 单例共通的缺陷。

---

## 六、对比总览表

| 对比维度 | 用户回答 | 标准回答 | 改进建议 |
|----------|----------|----------|----------|
| **QPC 基本理解** | ✅ 准确 | ✅ 准确 | 补充区分 `NowUs`(绝对时间) vs `NowMs`(增量时间) |
| **QPC 缺点深度** | ⚠️ 停留在"不好扩展"的功能层 | ✅ 耦合/测试/生命周期 | **核心差距**：需从功能视角切换到设计视角 |
| **SDL 基本理解** | ✅ 准确 | ✅ 准确 | 补充 `Start()` 的阻塞行为描述 |
| **SDL 缺点深度** | ⚠️ 只说"不易扩展" | — | **核心差距**：遗漏阻塞、join 无超时、职责耦合三个关键点 |
| **回答层次** | 功能层面（好不好用） | 设计层面（耦合/可测试性） | **关键提升方向**：从"好不好扩展"转向"为什么这样设计不好" |
| **综合评分** | ⭐⭐⭐ | — | 方向正确，深度不足，需补设计视角 |

---

## 七、核心经验总结

1. **面试官问"优缺点"时，默认期望听到设计层面的反思**，而不是功能层面（好不好用、能不能扩展）。
2. **三个必答关键词**：耦合（coupling）、可测试性（testability）、生命周期安全（lifetime safety）。
3. **单例 = 隐式全局依赖**，这让代码简单但不灵活，面试时要能把这两面都说清楚。
4. **读源码要关注语义细节**：比如 `NowMs` 返回"增量"而非"绝对时间"，面试时能讲出这种区分是加分项。
5. **如果项目已经有了更好的方案**（如 `ITimestampProvider` 接口），一定要答出来，这体现你对架构演进的思考。
