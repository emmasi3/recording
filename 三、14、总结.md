# 三、14、音频采集链路与编码包处理 — 总结

---

## 一、问题

**14. 音频采集链路是怎样的？从麦克风到 AAC 编码包，经过了哪些处理？**

> 该问题实际包含两个子问题：
> - (1) 麦克风 → AAC 编码包的完整采集与处理链路
> - (2) AAC 编码包的后继处理流程（如何写入容器/推流）

---

## 二、用户原始回答

### （1）音频采集链路

> 该项目音频采集链路：首先通过 ffmpeg 接口创建设备上下文并打开麦克风设备；在初始化音频采集需要的相关资源之后，开启音频读取线程，读取一帧 AVPacket 包，送入解码器解码出原始 PCM 数据，随即送入音频重采样器进行重采样，符合音频编码器的期望属性，之后放入音频共享队列，也就是生产者队列；之后在消费端，通过封装 ffmpeg_audio_fifo 的相关接口，从队列中取出一帧音频数据（共享队列保证一次取出的数据采样数为编码器期望的大小），并进行编码工作，接收一帧编码后的音频包，并调用用户传入的回调函数，处理音频包。

### （2）音频编码包的处理流程

> 如（1）中所示。

---

## 三、标准参考回答

### （1）音频采集链路

> DShow 麦克风 → `avformat_open_input("audio=设备名")` → `av_read_frame` 读原始 PCM 包 → `avcodec_send_packet` / `avcodec_receive_frame` 解码为 AVFrame → `swr_convert` 重采样（采样率/格式/声道对齐编码器要求）→ `AudioFifoQueue::Push` → 消费者从 FIFO 取满 `nb_samples`（如 1024）拼成 AVFrame → 送入 `libfdk_aac` → `avcodec_receive_packet` 拿 AAC 包。

### （2）音频编码包处理

> 编码后的 AAC 包通过回调函数处理，内部调用 `av_packet_rescale_ts` 将时间戳从编码器时间基转换为输出流时间基，最终通过 `av_interleaved_write_frame` 写入 FLV/MP4 容器，由 FFmpeg 自动完成音视频交错排序。

---

## 四、用户回答 vs 标准回答 对比分析

| 环节 | 用户回答 | 标准回答 | 分析 |
|------|---------|---------|------|
| **设备打开** | "通过 ffmpeg 接口创建设备上下文并打开麦克风设备" | `avformat_open_input("audio=设备名")` | ✅ 方向对，但缺少具体 API 名称 |
| **读取帧** | "读取一帧 AVPacket 包" | `av_read_frame` 读原始 PCM 包 | ⚠️ DShow 下读出来的是原始 PCM，非编码帧 |
| **解码** | "送入解码器解码出原始 PCM 数据" | `avcodec_send_packet` / `avcodec_receive_frame` | ⚠️ 未提具体 API 配对 |
| **重采样** | "送入音频重采样器进行重采样" | `swr_convert` 重采样 | ⚠️ 未提具体 API（`swr_convert` + `swr_alloc_set_opts2`） |
| **入队** | "放入音频共享队列，也就是生产者队列" | `AudioFifoQueue::Push` | ⚠️ "音频共享队列"不够精确——这是采样点级 FIFO（`AVAudioFifo`），非普通帧级队列 |
| **出队取帧** | "从队列中取出一帧音频数据（保证一次取出的数据采样数为编码器期望的大小）" | 消费者从 FIFO 取满 `nb_samples`（如1024）拼成 AVFrame | ✅ 核心亮点描述准确 |
| **编码** | "进行编码工作，接收一帧编码后的音频包" | 送入 `libfdk_aac` → `avcodec_receive_packet` | ⚠️ 未提编码器名称和 API |
| **回调** | "调用用户传入的回调函数，处理音频包" | （标准回答未提及，但代码确实有此设计） | ✅ 这是用户回答独有的亮点 |
| **PTS 时间戳** | 未提及 | `av_packet_rescale_ts` 转换时间基 | ❌ 缺失 |
| **写入容器** | 未提及 | `av_interleaved_write_frame` | ❌ 缺失 |

---

## 五、关键 API 速查表

面试中建议能脱口而出的关键 API：

| 步骤 | 关键 API / 技术点 |
|------|------------------|
| 设备发现 | `avdevice_list_input_sources` 枚举 DShow 设备，自动拼接 `"audio=设备名"` |
| 设备打开 | `avformat_open_input(&ctx, "audio=xxx", ifmt, nullptr)` |
| 读取帧 | `av_read_frame(m_FmtCtx, pkt)` |
| 解码 | `avcodec_send_packet` → `avcodec_receive_frame` |
| 重采样 | `swr_alloc_set_opts2` + `swr_convert` |
| 入队 | `av_audio_fifo_write`（封装在 `AudioFifoQueue::Push`） |
| 出队 | `av_audio_fifo_read`（封装在 `AudioFifoQueue::WaitAndPop`），精确取 `nb_samples` 个采样点 |
| 编码 | `avcodec_send_frame` → `avcodec_receive_packet`（AAC） |
| PTS 缩放 | `av_packet_rescale_ts(pkt, 编码器时间基, 输出流时间基)` |
| 写入容器 | `av_interleaved_write_frame`（自动音视频交错） |

---

## 六、生产者 / 消费者线程分界

用户回答未明确区分的两个线程阶段：

```
┌─────────────────────────────────────────────────────────────────┐
│   生产者线程（DshowAudioCapture::ReadFrameFrom_device，第120行）   │
│                                                                  │
│   读帧 → 解码 → swr_convert → AudioFifoQueue::Push               │
│                                                                  │
│   退出前: FlushAudioDecoder() — 送 nullptr → 循环 receive_frame   │
│           → 重采样 → 入队，确保尾部数据不丢失                       │
└──────────────────────────────┬──────────────────────────────────┘
                               │ AudioFifoQueue（采样点级 FIFO）
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│   消费者侧（DshowAudioCapture::ReadFrame，由 MuxThreadProc 调用）  │
│                                                                  │
│   WaitAndPop → 取满 nb_samples(1024) → AudioFfmpegEncoder::Encode │
│   → callback → av_interleaved_write_frame                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 七、优化版完整回答（供背诵参考）

> **音频采集链路（麦克风 → AAC 编码包）：**
>
> **生产端**（`DshowAudioCapture::ReadFrameFrom_device` 线程）：
> 1. 通过 `avdevice_list_input_sources` 枚举 DShow 音频设备，拼接 `"audio=设备名"` 后用 `avformat_open_input` 打开麦克风
> 2. `av_read_frame` 循环读取原始 PCM 的 AVPacket（DShow 设备直接吐 PCM，非编码帧）
> 3. 送入音频解码器：`avcodec_send_packet` → `avcodec_receive_frame`，解码为 AVFrame      
> 4. 送入音频重采样器 `swr_convert`：将解码器输出的采样率/格式/声道布局统一转换为 AAC 编码器要求的格式（如 44100Hz、FLTP、立体声）。
     重采样时用 `swr_get_delay` 加上内部缓存的延迟样本数，精确计算输出采样数
> 5. 重采样后的 AVFrame 通过 `AudioFifoQueue::Push`（底层封装 `av_audio_fifo_write`）写入采样点级 FIFO 队列
> 6. 线程退出前调用 `FlushAudioDecoder()`：向解码器送 `nullptr` 触发 flush，循环 `receive_frame` 拿残留帧并同样走重采样→入队，确保尾部数据不丢失
>
> **消费端**（`DshowAudioCapture::ReadFrame`，由 `MuxThreadProc` 调用）：
> 1. `AudioFifoQueue::WaitAndPop(m_frame)`（底层 `av_audio_fifo_read`）阻塞等待并精确取出编码器要求的 `nb_samples`（如 1024）个采样点，拼成完整的 AVFrame
> 2. 送入 `AudioFfmpegEncoder::Encode(frame, callback)`：设置 PTS = `m_nbSamples × frame_index`，`avcodec_send_frame` 送入 `libfdk_aac` 编码器
> 3. 遇到 `EAGAIN` 时先 `avcodec_receive_packet` 消费就绪包以释放编码器内部缓冲，然后重试 send_frame
> 4. 收到编码包后，通过 `av_packet_rescale_ts` 将时间基从编码器 `{1, sample_rate}` 转换为输出流 `{1, 1000}`，最终通过回调函数调用 `av_interleaved_write_frame` 写入容器
>
> **音频编码包处理流程（回调链路）：**
> - 回调函数在 `MuxThreadProc` 中绑定为 `av_interleaved_write_frame`，将 AAC 包写入 FLV/MP4 容器
> - `av_interleaved_write_frame` 根据音视频包 PTS 自动交错排序，保证输出文件音视频按时间戳交替排列

---

## 八、深度追问：`swr_get_delay` 的作用与不使用它的后果

### 8.1 什么是 `swr_get_delay`？

```cpp
int swr_get_delay(SwrContext *s, int64_t base);
```

返回重采样器**内部缓冲区中尚未输出的采样点数**（以 `base` 指定的采样率为单位）。

### 8.2 为什么重采样器有内部缓存？

重采样不是简单的"输入 N → 输出 M"一一映射。以 44100Hz → 48000Hz 为例，比率是 1.088...，每次调用 `swr_convert` 时，内部的多相滤波器需要一定数量的历史样本做插值，且由于比率不是整数，总有一些"不够凑成完整输出样本"的残余留在内部缓冲区。

### 8.3 项目中的正确做法（动态策略）

```cpp
// 根据当前内部缓存 + 输入帧的采样数，精确计算本次期望的输出的采样数
dstNbSamples = av_rescale_rnd(
    swr_get_delay(m_swrCtx, dec_sample_rate) + frame->nb_samples,
    enc_sample_rate,
    dec_sample_rate,
    AV_ROUND_UP
);

// 如果输出缓冲不够大，则重新分配
if (dstNbSamples > maxNbSamples) {
    av_frame_free(&newFrame);
    newFrame = AllocAudioFrame(dstNbSamples);
}
```

### 8.4 如果始终使用固定缓冲区（不调用 `swr_get_delay`），会发生什么？

#### 阶段 1：初始无延迟，侥幸正常

| 迭代 | 输入样本 | 内部延迟 | 期望输出 | 固定缓冲 | 结果 |
|------|---------|---------|---------|---------|------|
| 1 | 1002 | 0 | ~1024 | 1024 | ✅ 正常 |
| 2 | 1044 | ~2 | ~1064 | 1064 | ✅ 正常（但内部已残留微量样本） |

#### 阶段 2：延迟悄然累积

| 迭代 | 输入 | 内部延迟 | 期望输出 | 固定缓冲 | 实际输出 | 新残留 |
|------|-----|---------|---------|---------|---------|-------|
| 3 | 1002 | 2 | ~1026 | **1024** ❌ | **1024** | **~4** |
| 4 | 1044 | 4 | ~1066 | **1064** ❌ | **1064** | **~6** |
| 5 | 1002 | 6 | ~1030 | **1024** ❌ | **1024** | **~12** |

每次"装不下"的样本永久留在重采样器内部，`swr_get_delay` 持续增长。

#### 阶段 3：延迟失控

```
录制 10 分钟 → 内部缓存积压 0.5~2 秒音频
录制 1 小时  → 内部缓存积压 5~10 秒音频
```

#### 四大灾难性后果

| 后果 | 说明 |
|------|------|
| **音频延迟线性增长** | 对着麦克风说话，10 秒后才在推流/录制中听到 —— 实时推流场景完全不可用 |
| **输出帧 PTS 系统性偏差** | 编码器每帧仍是 1024 采样点，但这些采样点对应的**实际采集时刻已发生偏移**，PTS 与真实时间脱节 |
| **录制结束时的"音频海啸"** | Flush 阶段，重采样器积压的几千个采样点一次性吐出，文件尾部出现"音频比视频多好几秒"的诡异 AV 不同步 |
| **潜在缓冲区溢出** | 延迟持续增长到 FFmpeg 内部上限后，可能返回错误或行为未定义 |

### 8.5 一句话总结

> 不调用 `swr_get_delay` 来动态更新输出缓冲区大小，等于**假装重采样器没有内部状态**。由于采样率比率的非整数特性，每次 `swr_convert` 都会留下微量残余，这些残余像"滚雪球"一样持续累积，最终导致音频延迟线性增长、AV 同步崩溃，对实时推流场景是**致命的**。

### 8.6 对比总结

| 维度 | 固定缓冲区（不更新） | 动态更新（当前做法） |
|------|--------------------|--------------------|
| 延迟 | 线性累积，无限增长 | 始终 ≤ 1 帧的抖动量 |
| nb_samples | 不可预测，偏差累积 | 精确可控 |
| 内存 | 内部缓冲区持续膨胀 | 外部缓冲区按需扩缩，总量可控 |
| 长时录制 | AV 逐渐不同步 | 稳定同步 |
| Flush 尾部 | "音频海啸"，大量残留帧 | 正常结束，至多几帧残留 |
| 实时推流 | 不可用 | 可用 |

---

## 九、面试要点 Checklist

- [ ] 能说出完整的采集链路：设备打开 → 读帧 → 解码 → 重采样 → FIFO 入队 → 出队 → 编码 → 回调 → 写入容器
- [ ] 能说出每个环节的关键 API 名称（见第五章速查表）
- [ ] 能明确区分**生产者线程**和**消费者线程**的职责边界
- [ ] 能解释 `AudioFifoQueue` 为什么是采样点级 FIFO 而非帧级队列
- [ ] 能说明重采样参数：采样率、采样格式（如 S16→FLTP）、声道布局
- [ ] 能解释 `swr_get_delay` 的作用及不使用它的严重后果
- [ ] 能说明 `FlushAudioDecoder` 的时机（线程退出前）和内部流程（送 nullptr → 循环 receive → 重采样 → 入队）
- [ ] 能说出编码后 PTS 的缩放过程：`av_packet_rescale_ts` 从编码器时间基转换为输出流时间基
- [ ] 能解释 `av_interleaved_write_frame` 的自动音视频交错机制
- [ ] 能提及 `EAGAIN` 在音频编码中的特殊处理策略（先 receive_packet 再重试 send_frame）
