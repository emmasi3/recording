# 四、21 — `AV_CODEC_FLAG_GLOBAL_HEADER` 与 RTMP 推流

---

## 问题

`AV_CODEC_FLAG_GLOBAL_HEADER` 是什么？项目中为什么要设置它？和 Nginx-RTMP 报错 `"invalid video codec header size=5"` 有什么关系？裸 `.h264` 文件为什么不能设置该标志？

---

## 用户的原始回答

> **(1)** `AV_CODEC_FLAG_GLOBAL_HEADER` 是视频编码器上下文的一个标志，如果该标志启用，那么编码器给出的**每一帧视频头部，都会带有**该视频的一些头信息，主要是 SPS、PPS 这些头信息。如果仅仅是用于本地，那么选择该标志，会节省一部分内存，只需要头部有就行。
>
> **(2)** 但是该项目是要在实时流中表现的，通过 FLV 格式、通过 RTMP 协议标准实现直播效果，拉流端或者服务端，在处理这些信息时，**必须依靠每一帧头部的信息**；如果仅仅有一个总的头信息，那么实时流发生一些变化时，服务端和拉流端可能无法察觉。就如同 Nginx 的 RTMP 模块，在解析 H.264 包时，必须要有头信息，否则就会报错：`"invalid video codec header size=5"`，意思是无效的视频头信息，一般来说，这种错误是解析时没有 SPS、PPS 信息没找到，或者这些信息本身就是错误的。

---

## 用户回答中的两个核心错误

### 错误①：标志含义说反了

用户认为"设置该标志 → 每帧都带 SPS/PPS"，**实际正好相反**：

| 设置 GLOBAL_HEADER？ | SPS/PPS 存放位置 | 每帧数据中是否携带 SPS/PPS |
|:---:|---|---|
| ✅ **设置**（项目中这么做） | `extradata`（编码器上下文全局字段） | ❌ 不携带，帧数据只有图像数据 |
| ❌ 不设置 | 嵌入在 IDR 关键帧的 `AVPacket.data` 中 | ✅ 携带（裸流模式） |

**核心逻辑**：`AV_CODEC_FLAG_GLOBAL_HEADER` 的本质是**"把 SPS/PPS 提到容器头部，与帧数据分离"**，而不是"让每帧都带 SPS/PPS"。

### 错误②：本地 vs 实时流的适用场景说反了

用户认为"本地文件用该标志，实时流必须每帧都有头部信息"——**实际场景正好相反**：

| 实际场景 | 是否设置 GLOBAL_HEADER | 原因 |
|---|---|---|
| **RTMP 推流（FLV 封装）** | ✅ **必须设置** | FLV 播放器从容器头部（metadata tag）拿 SPS/PPS，不设置则 header 为空 → Nginx 解析失败 |
| **裸 `.h264` 文件** | ❌ **不能设置** | 裸流没有容器头部，播放器只能从帧数据中读 SPS/PPS；设置后 SPS/PPS 进了 extradata 但裸流不会写 extradata → 解码器无法初始化 |

---

## 标准参考回答

### (1) `AV_CODEC_FLAG_GLOBAL_HEADER` 的作用

它控制 H.264/H.265 编码器将 SPS/PPS 等序列参数信息放入**哪里**：

- **设置该标志**：SPS/PPS 写入编码器上下文的 `extradata` 字段，编码后的每帧 AVPacket.data 中**不包含** SPS/PPS。在 `avformat_write_header()` 时，FFmpeg 从 `extradata` 读取并写入 FLV/MP4 容器的头部。
- **不设置该标志**：SPS/PPS 嵌入在每帧数据中（IDR 关键帧的 AVPacket.data 包含它们），形成"每帧自包含"的裸流格式。

### (2) 为什么 RTMP/FLV 推流必须设置，而裸 `.h264` 不能设置？

- **RTMP 推流**：FLV 格式的播放器和 Nginx-RTMP 服务器在解析 H.264 流时，期望从容器头部（metadata tag）获取 SPS/PPS。如果不设置 `GLOBAL_HEADER`，`avformat_write_header()` 写出的头部为空，Nginx 解析不到有效的视频描述信息，直接报 `"invalid video codec header size=5"` 并断开连接。
- **裸 `.h264` 文件**：裸流没有容器头部，播放器只能从帧数据中读取 SPS/PPS。如果设置了 `GLOBAL_HEADER`，SPS/PPS 被放入 `extradata` 但裸流文件不会写 `extradata`，播放器打开后找不到序列参数信息，无法初始化解码器 → 无法播放。

### 数据流对比

```
【设置 GLOBAL_HEADER（RTMP/FLV 场景）】
编码器 → extradata（SPS/PPS 存这里）
        → AVPacket.data（只有图像数据，不含 SPS/PPS）
avformat_write_header() → 从 extradata 写入 FLV header → Nginx 正确解析 ✅

【不设置 GLOBAL_HEADER（裸 .h264 场景）】
编码器 → extradata 为空
        → AVPacket.data（图像数据 + SPS/PPS 一起）
avformat_write_header() → extradata 为空 → Nginx 解析失败 ❌
                         → "invalid video codec header size=5"
```

---

## 项目代码验证

项目中 `FfmpegEncoder.cpp` 第 299-304 行的注释和代码明确体现了上述逻辑：

```cpp
/*
* 设置编码器上下文的标志位，若不添加该标志，会导致 Nginx-rtmp 服务器直接返回
  "codec: invalid video codec header size=5"。
* 但是对于 .h264 这种格式的文件，该选项不能够勾选，每一帧必须有单独的 header-info，
  否则大多数播放器无法解析 .h264 文件，
* 但是对于大多数封装格式 mp4、flv、mkv、mov...只需要开始有一个 header-info，
  就可以解析完成了，这和具体的格式有关
*/
m_ctx->flags |= AV_CODEC_FLAG_GLOBAL_HEADER;
```

**代码解读**：
- 第 2 行：裸 `.h264` 文件不能勾选 —— 因为"每一帧必须有单独的 header-info"
- 第 4-5 行：FLV/MP4 等封装格式只需要"开始有一个 header-info"即可 —— 即从 `extradata` 写入容器头部的那一次

---

## 核心对比总结表

| 对比维度 | 用户回答（错误理解） | 标准回答（正确理解） |
|---|---|---|
| 标志含义 | 设置后每帧都带 SPS/PPS | 设置后 SPS/PPS 进 extradata，每帧不带 |
| 裸 .h264 | 可以设置 | **不能设置**，否则播放器找不到 SPS/PPS |
| RTMP 推流 | 认为实时流需要每帧头部 | **必须设置**，让 SPS/PPS 写入 FLV 容器头部 |
| Nginx 报错根因 | 没找到每帧中的 SPS/PPS | `avformat_write_header()` 时 extradata 为空，FLV header 缺少 SPS/PPS |
| 本质 | 把概念理解反了 | GLOBAL_HEADER = "SPS/PPS 提到容器头部，与帧数据分离" |

> **一句话记忆**：`GLOBAL_HEADER` = 把 SPS/PPS"提到全局头部"（extradata）→ 容器头部一次性写入 → FLV/MP4 需要它，裸 `.h264` 不能有它。

---

## 面试追问点

1. **音频编码器（AAC）是否也需要设置 `GLOBAL_HEADER`？**
   > 是的。AAC 的 `AudioSpecificConfig`（音频特定配置，包含采样率、声道数等）也是通过 `extradata` 机制写入容器头部的。项目中音频编码器也设置了该标志。

2. **如果不小心对裸 `.h264` 文件设置了 `GLOBAL_HEADER`，后果是什么？**
   > 播放器打开后无法获取 SPS/PPS（它们只存在于 `extradata` 中，但裸流文件的写入路径不会使用 `extradata`），无法获知分辨率、帧率等参数 → 无法初始化解码器 → 直接报错无法播放。

3. **`avformat_write_header()` 除了写 SPS/PPS，还做了什么？**
   > 写入 FLV header（文件签名 `FLV`）、metadata tag（宽高、帧率、码率、编码格式）、以及从各流 `extradata` 读取的 codec 配置信息。这些是播放器初始化所必需的。
