# 三、16、总结 — AudioFifoQueue 生产者-消费者模式

---

## 一、面试问题

> 请介绍项目中 `AudioFifoQueue` 的设计思路，包括它采用了什么并发模式、两个条件变量各自的语义和作用，以及它们如何协同保证音频数据的正确、流畅传输。

---

## 二、用户原始回答

> （1）首先这是一个生产者-消费者模式，以 `AudioFifoQueue` 为生产者-消费者之间的数据桥梁，生产者负责填充、消费者负责读取并处理音频数据；
>
> （2）`m_cvNotEmpty`、`m_cvNotFull` 这两个条件变量语义分别为：不空、不满，分别对应生产、消费两端的数据读取阻塞逻辑；
>
> **m_cvNotEmpty**：通过当前缓冲区中的采样点数是否满足一帧音频数据期望，如果不满足，就释放锁、阻塞等待，等待下次唤醒；
>
> **m_cvNotFull**：生产者线程准备写入音频包时，如果此时缓冲区空间能够容纳期望的采样点数，直接放入，反之，进行阻塞等待，直到通知下发后再次检查；
>
> 两者共同保证生产、消费的正确性、流畅性。

---

## 三、标准参考回答

### 3.1 整体架构：生产者-消费者模式

`AudioFifoQueue` 采用经典的**生产者-消费者模式**，以 FFmpeg 的 `AVAudioFifo` 作为底层缓冲区，生产者（音频采集/重采样线程）通过 `Push()` 写入采样点数据，
    消费者（音频编码线程）通过 `WaitAndPop()` 读取固定数量的采样点，两端通过两个条件变量协同调度，实现解耦与流水线化。

> 核心源码位于：
> - `include/concurrency/ConcurrentQueue.h`（第 200–283 行）：`AudioFifoQueue` 类声明
> - `src/concurrency/ConcurrentQueue.cpp`（第 70–128 行）：`Push()` 实现
> - `src/concurrency/ConcurrentQueue.cpp`（第 143–164 行）：`WaitAndPop()` 实现

### 3.2 两个条件变量详解

| 条件变量 | 语义 | 等待方 | 谓词（唤醒条件） | 通知方 |
|---------|------|--------|---------------|--------|
| `m_cvNotEmpty` | 队列**不空** | 消费者（`WaitAndPop`） | `av_audio_fifo_size(m_aFifoBuf) >= item->nb_samples`（FIFO 中采样点数 ≥ 编码器一帧所需） | 生产者（`Push` 写入后调 `notify_one`） |
| `m_cvNotFull` | 队列**不满** | 生产者（`Push`） | `av_audio_fifo_space(m_aFifoBuf) >= item->nb_samples`（FIFO 剩余空间 ≥ 待写入采样点数） | 消费者（`WaitAndPop` 读取后调 `notify_one`） |

#### 工作流程

```
生产者 Push()                              消费者 WaitAndPop()
    │                                           │
    ├─ 1. 加锁                                  ├─ 1. 加锁
    ├─ 2. 积压丢弃检查（>2秒则 drain）            ├─ 2. m_cvNotEmpty.wait()
    ├─ 3. 空间不足则扩容                          │     等待 FIFO 采样点数 ≥ nb_samples
    ├─ 4. m_cvNotFull.wait()                     ├─ 3. av_audio_fifo_read() 读取
    │     等待剩余空间 ≥ nb_samples               ├─ 4. 解锁
    ├─ 5. av_audio_fifo_write() 写入             │
    ├─ 6. 解锁                                   ├─ 5. m_cvNotFull.notify_one()
    │                                           │     （通知生产者"有空间了"）
    └─ 7. m_cvNotEmpty.notify_one()             └─
           （通知消费者"有数据了"）
```

### 3.3 与普通 BlockingQueue 的区别（加分项）

| 特性 | `BlockingQueue<AVFrame*>` | `AudioFifoQueue` |
|------|--------------------------|-----------------|
| 数据粒度 | **帧级**：队列单元是完整 `AVFrame` | **采样点级**：底层为 `AVAudioFifo`，以采样点为最小单位 |
| 入队方式 | 整个 `AVFrame` 指针入队 | `av_audio_fifo_write()` 拷贝采样点数据到 FIFO |
| 出队方式 | 取出完整帧 | `av_audio_fifo_read()` 精确取出 `nb_samples` 个采样点，构造新帧 |
| 适用场景 | 视频帧等大小不确定的数据 | 音频帧，需每次取固定采样点数（如 1024）送入编码器 |

### 3.4 工程化考量：延迟丢弃策略（背压 + 防 OOM）

#### 什么是 OOM？

**OOM** = **Out of Memory**（内存溢出）。当生产者（采集线程）持续向 `AVAudioFifo` 写入采样点数据，而消费者（编码/推流线程）因网络卡顿等原因消费过慢时，FIFO 内部缓冲区会不断膨胀：

```
采样点数随时间变化：
1024 → 4096 → 16384 → 65536 → 262144 → ... → 耗尽系统内存 → 程序崩溃
```

`AVAudioFifo` 底层使用动态内存分配，若不加限制，采样点持续堆积最终会超出系统可用内存，`malloc` 失败导致程序崩溃——这就是 **OOM 现象**。

#### 代码中如何防 OOM？

`Push()` 中这段逻辑是核心防线（`src/concurrency/ConcurrentQueue.cpp` 第 81–96 行）：

```cpp
// 最大容忍延迟积压设为 2 秒（约 88200 个采样点 @44100Hz）
int max_samples_limit = sampleRate * 2;

// 如果积压超过 1 秒（max_samples_limit / 2），主动丢弃最旧数据
if (curSize > (max_samples_limit / 2))
{
    int drain_size = (curSize + item->nb_samples) - (max_samples_limit / 2);
    if (drain_size > 0)
    {
        av_audio_fifo_drain(m_aFifoBuf, drain_size);  // 丢弃队尾最旧的数据
    }
}
```

**设计思路**：

| 策略 | 说明 |
|------|------|
| **积压上限** | 设置 2 秒采样点硬上限（如 44100Hz 时为 88200 个采样点） |
| **主动丢弃** | 超过 1 秒阈值即触发 `drain`，丢弃最旧的音频数据 |
| **降级保活** | 宁可短暂静音/卡顿，也绝不让内存无限膨胀导致崩溃 |

**总结**：`m_cvNotFull` 条件变量是第一道防线（阻塞生产者等待空间），而 `drain` 主动丢弃是最后一道防线——二者共同确保低延迟的同时防止 OOM 崩溃。

### 3.5 为什么用两个条件变量而不是一个？

- **定向唤醒**：消费者只需感知"队列不空"，生产者只需感知"队列不满"，避免惊群效应；
- **语义清晰**：代码可读性更强，`notify_one` 只唤醒真正关心的那一方；
- **避免无效唤醒**：单条件变量下，生产者被唤醒可能发现空间仍不足（被其他生产者抢先），需要额外循环判断——两个条件变量从根本上避免了这类问题。

---

## 四、要点速查表

| 编号 | 核心要点 | 一句话记忆 |
|------|---------|-----------|
| 1 | 模式 | 生产者-消费者，以 `AVAudioFifo` 为桥梁 |
| 2 | `m_cvNotEmpty` | 消费者侧："有足够数据吗？没有就等" |
| 3 | `m_cvNotFull` | 生产者侧："空间够吗？不够就等" |
| 4 | 数据粒度 | **采样点级 FIFO**，区别于普通帧级队列 |
| 5 | 背压 | 超出 2 秒积压则主动 drain，防 OOM + 低延迟 |
| 6 | 双条件变量优势 | 定向唤醒，避免惊群，语义清晰 |
