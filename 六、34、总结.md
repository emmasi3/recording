# 六、34 — 项目的重连策略是什么？怎么用的？

---

## 一、问题原文

> **34. 项目的重连策略是什么？怎么用的？**

该问题考察候选人对推流工程中断线恢复机制的完整理解，包括：项目现有实现、断连检测、`AVFormatContext` 生命周期管理、重连流程设计、PTS/IDR 帧处理、退避策略等。

---

## 二、用户原始回答

> 项目中并没有设计这样的一个推流重连的策略；如果需要，那么就需要分两种大情况：
>
> **（1）被动断连（网络 / 服务端主动断开）：**
> 由于网络不稳定、Nginx 超时等问题，服务端断开了连接，那么此时可以依靠 ffmpeg 的 API —— `avio_open2()` 进行再一次的连接，再次连接成功后，重复之后的流程即可，设计起来应该不复杂。
>
> **（2）主动断连（推流端主动暂停 / 恢复）：**
> 推流端主动断开连接，设置相应的用户可控的 QT 类型的"button"，实现随时恢复推流的功能，和第一种的大致流程一样，只不过第一种是被动的、断连后自动的，而第二种是主动的。

---

## 三、评估总览

| 维度 | 评分 | 核心问题 |
|------|:----:|----------|
| 准确性与技术深度 | ★★☆ | `avio_open2()` 不能直接重连，缺少 `AVFormatContext` 重建的完整步骤，未提及 IDR / PTS / 积压帧等关键坑 |
| 逻辑结构 | ★★★ | 两种场景分类方向正确，但缺乏层次和细节 |
| 表达专业度 | ★★☆ | 省略号、引号滥用、"不复杂"等口语化表达 |
| 关键要点覆盖 | ★☆☆ | 遗漏 9 个关键点，其中 4 个为致命 / 严重级别 |

---

## 四、逐项诊断

### 4.1 致命错误：`avio_open2()` 不能直接用于重连

- **问题**：一旦 `av_interleaved_write_frame()` 返回错误（如 `WSAECONNRESET -10054`），`AVFormatContext` 内部状态已损坏，不可复用。
- **正确做法**：必须先 `avformat_free_context()` 销毁旧上下文，再从头重建。完整重建流程共 **6 步**（见下表）。

| 步骤 | API / 操作 | 说明 |
|:--:|------|------|
| ① | `avformat_free_context()` | 销毁已断连的 AVFormatContext（含内部 streams、pb） |
| ② | `avformat_alloc_output_context2()` | 重新分配输出上下文（指定 `"flv"`） |
| ③ | `avformat_new_stream()` + `avcodec_parameters_from_context()` | 重新创建音视频流，从编码器拷贝 codecpar |
| ④ | `avio_open2()` | **此时才可重新打开 RTMP 网络 IO** |
| ⑤ | `avformat_write_header()` | 重新写入 FLV header + SPS/PPS |
| ⑥ | 恢复 `av_interleaved_write_frame()` 循环 | 继续推流 |

### 4.2 遗漏的关键技术要点（共 9 项）

| # | 遗漏要点 | 严重程度 | 说明 |
|:-:|----------|:--------:|------|
| 1 | 断连检测机制 | 🔴 致命 | 未说明如何判断已断连（`av_interleaved_write_frame` 负数返回值 / `AVERROR(EIO)` / `WSAECONNRESET`） |
| 2 | `AVFormatContext` 重建的完整 6 步 | 🔴 致命 | 只提 `avio_open2`，完全遗漏 free → alloc → new_stream → params → write_header |
| 3 | 重连后首帧必须是 IDR 关键帧 | 🔴 严重 | RTMP 播放端需从 SPS/PPS + IDR 重新解码，必须强制编码器输出 I 帧 |
| 4 | PTS/DTS 连续性 | 🔴 严重 | 断连后 PTS 是从 0 重新开始还是继续？需明确说明 |
| 5 | 积压帧处理 | 🟡 中级 | 断连期间采集线程仍在 Push，过期帧必须丢弃 |
| 6 | 已有 `ExponentialReconnectPolicy` | 🟡 中级 | 面试官问"你的项目"，项目已定义接口却未提及 |
| 7 | 重试上限 | 🟡 中级 | 无限重连导致资源泄漏和死循环，需最大次数或超时兜底 |
| 8 | 线程安全 | 🟡 中级 | `MuxThreadProc` 中状态切换需 `std::atomic` 标记 |
| 9 | Nginx 服务端超时 | 🟢 低级 | 服务端 keepalive 超时需与重连间隔配合 |

### 4.3 项目已有基础（回答中未提及）

项目中其实已经定义了重连策略的抽象接口和默认实现：

```cpp
// include/core/ErrorPolicy.h

class IReconnectPolicy {
public:
    virtual ~IReconnectPolicy() = default;
    virtual uint32_t NextBackoffMs() = 0;  // 计算下次重连等待毫秒数
    virtual void Reset() = 0;              // 成功后重置退避
};

class ExponentialReconnectPolicy final : public IReconnectPolicy {
public:
    uint32_t NextBackoffMs() override;
    void Reset() override;
private:
    uint32_t currentMs_{500};   // 初始 500ms
    uint32_t maxMs_{8000};      // 最大 8000ms
};
```

- **NextBackoffMs()**：每次调用返回当前值，然后翻倍（上限 8000ms）
- **Reset()**：成功后重置回 500ms

> 但这只是"预留扩展点"——`MuxThreadProc` 推流主循环中**并未实际接入**断线重连逻辑。遇到错误直接退出，不重试。

### 4.4 表达规范性改进对照

| 用户原表述 | 问题 | 建议改为 |
|----------|------|----------|
| `····` | 不规范的省略号 | 使用正式标点或直接说"等" |
| `QT 类型的 "button"···` | 引号乱用、表述业余 | 「用户可控的 UI 控件（如 Qt 的暂停/恢复按钮）」 |
| `设计起来应该不复杂` | 主观臆断，显幼稚 | 「重连的核心流程可以抽象为通用函数，两种场景共用」 |
| `依靠 ffmpeg 的 API -- avio_open2()` | 破折号不规范 | 「通过 ffmpeg 的 `avio_open2()` API」 |

---

## 五、标准参考回答

> **当前状态：** 项目中已定义了重连策略接口 `IReconnectPolicy` 及其默认实现 `ExponentialReconnectPolicy`（初始 500ms，每次翻倍，最大 8000ms，`Reset()` 在成功后重置）。但在 `MuxThreadProc` 中并未实际接入断线重连逻辑——推流线程遇到 `av_interleaved_write_frame` 返回错误后直接退出，没有重试。这是架构演进中的预留扩展点。
>
> **完整重连方案应分两种场景，但核心流程可抽象复用：**

---

### 触发层：断连检测

`av_interleaved_write_frame()` 返回负数（典型值如 `AVERROR(EIO)`、`WSAECONNRESET -10054`），或通过 `avio_feof(m_ctx->pb)` 检测。Nginx 日志端可辅助确认（如 `"codec: invalid video codec header size=5"` 为 header 级断连，不同于网络超时）。

---

### 场景一：被动断连（网络波动 / 服务端超时）→ 自动重连

在 `MuxThreadProc` 的 `while(true)` 循环中，若检测到 `m_muxer->WritePacket(pkt)` 返回 false，进入重连状态机：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1. 清理 | `avformat_free_context()` | 释放已损坏的 `AVFormatContext` |
| 2. 退避 | `ExponentialReconnectPolicy::NextBackoffMs()` + sleep | 避免频繁重连打爆服务端 |
| 3. 重建 | 6 步重建上下文（见 4.1 表） | alloc → stream → params → avio_open → write_header |
| 4. 强制 IDR | `frame->pict_type = AV_PICTURE_TYPE_I` | 播放端需从 SPS/PPS + IDR 重新解码 |
| 5. PTS 归零 | 编码器计数器 Reset | RTMP 新会话，时间轴重新开始 |
| 6. 丢弃积压帧 | `BlockingQueue.drain()` | 断连期间已采集的过期帧全部 Pop |
| 7. 结果处理 | 成功 → `Reset()` 退避 + 恢复循环 | 失败 → 再次退避重试 |
| 8. 重试上限 | 超过 10 次或 60s 超时 | 记录错误并终止线程，避免死循环 |

---

### 场景二：主动暂停 / 恢复（用户手动控制）

与被动断连的核心区别在**触发方式**：

| 对比项 | 被动断连（自动） | 主动暂停（手动） |
|--------|:--:|:--:|
| 触发源 | `av_interleaved_write_frame` 返回错误 | UI 按钮信号（如 Qt `QPushButton`） |
| 等待方式 | 退避策略 sleep（指数递增） | `condition_variable::wait` 阻塞，直到用户点击恢复 |
| 触发时机 | 自动检测，不可预知 | 用户主动控制 |
| 核心流程 | 清理 → 退避 → 重建 → 恢复 | 清理 → 阻塞等待 → **复用同一套重建流程** |

两种场景共用同一套「清理 + 重建」的核心函数（如 `ReconnectMuxer()`），区别仅在于触发方式和等待策略。

---

## 六、一句话总结

> 你能意识到"被动 vs 主动"两个场景的区分，说明有一定产品思维；但技术深度严重不足——`avio_open2()` 不是重连的直接手段，重连涉及 **AVFormatContext 全生命周期重建**、**IDR 帧保证**、**PTS 归零**、**积压帧丢弃** 等多个硬核问题。建议至少亲手写一次重连代码，踩一遍坑，才能把这题答到位。
