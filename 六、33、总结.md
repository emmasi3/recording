# 六、33、总结 — RTMP 断开连接的根因与 `GLOBAL_HEADER` 实战踩坑

---

## 一、问题

> **33. RTMP 断开连接的根因和处理方案？项目中实际踩了什么坑？**

---

## 二、用户原始回答

（以下为用户原始回答要点整理）

### 2.1 调试过程（踩坑经历）

- 推流端程序直接报错，打印的错误信息只能定位到 `av_interleaved_write_frame`，无法定位到服务端。
- 最后打开终端运行 Nginx-RTMP 服务端时，才发现真正的问题：服务端提示 **"video header info 无效"**。
- 在网上查找资料后，确认是**头信息问题**——SPS/PPS 没有正确写入 FLV 头部。

### 2.2 解决方案

在设置编码器输出上下文时：

```cpp
m_ctx->flags |= AV_CODEC_FLAG_GLOBAL_HEADER;
```

### 2.3 用户对原理的理解

- 如果没有启用 `AV_CODEC_FLAG_GLOBAL_HEADER` 标志，每一帧音视频数据都会携带有 SPS/PPS 信息。
- 但 Nginx-RTMP 在解析 FLV 格式的实时流时，**仅仅看刚开始 client 给出的音视频元数据信息（SPS/PPS）**，之后不会再次关注。
- 因此 SPS/PPS 信息**必须在约定的位置被呈现**（即 FLV header 中的 extradata），否则 Nginx 就会解析失败。

---

## 三、标准参考回答

### 3.1 精简版（面试核心三步）

| 步骤 | 内容 |
|:--:|------|
| **① 现象** | 推流端 `av_interleaved_write_frame` 报错（WSAECONNRESET），Nginx 日志显示 `"codec: invalid video codec header size=5"` |
| **② 根因** | 未设置 `AV_CODEC_FLAG_GLOBAL_HEADER` → SPS/PPS 嵌在每帧数据中 → `extradata` 为空 → `avformat_write_header()` 无法将编码配置写入 FLV 头部 → Nginx 解析失败 → 主动断开连接 |
| **③ 解决** | `m_ctx->flags \|= AV_CODEC_FLAG_GLOBAL_HEADER`，将 SPS/PPS 从帧数据迁移到 `extradata` |

### 3.2 详细因果链（数据流视角）

#### 不设 `GLOBAL_HEADER` 时的数据流：

```
编码器
  ├── extradata → 空 ❌
  └── AVPacket.data → [SPS/PPS + 图像数据]（绑定在每帧内部）
        │
        ▼
avformat_write_header()
  │  从 extradata 读 codec 配置 → extradata 为空
  │  写入 FLV Script Tag → 缺少 SPS/PPS ❌
  │
  ▼
Nginx-RTMP 解析 FLV header
  └→ "codec: invalid video codec header size=5"
  └→ 主动断开 TCP 连接
  │
  ▼
客户端 av_interleaved_write_frame()
  └→ 尝试往已断开的 socket 写数据 → WSAECONNRESET (-10054)
```

#### 设了 `GLOBAL_HEADER` 之后：

```
编码器
  ├── extradata → [SPS/PPS] ✅
  └── AVPacket.data → [只有图像数据]（SPS/PPS 已分离出去）
        │
        ▼
avformat_write_header()
  │  从 extradata 读 codec 配置 → 有完整的 SPS/PPS
  │  写入 FLV Script Tag → 正确 ✅
  │
  ▼
Nginx-RTMP 解析 FLV header → 成功，正常推流
```

### 3.3 核心原理说明

**`AV_CODEC_FLAG_GLOBAL_HEADER` 的作用是控制 SPS/PPS 的存放位置：**

| 标志状态 | SPS/PPS 位置 | FLV 头部能否获取 | 结果 |
|----------|-------------|:---:|------|
| 未设置 | 嵌入每帧 `AVPacket.data` 内 | ❌ | `extradata` 为空，Nginx 解析失败 |
| 已设置 | 存入编码器 `extradata` 字段 | ✅ | FLV header 正确写入，推流正常 |

**关键认知**：问题不在于"Nginx 忽略每帧中的 SPS/PPS"，而是 **SPS/PPS 根本没被写入 FLV 头部**。`avformat_write_header()` 只从 `extradata` 读取 codec 配置来生成 FLV header（Script Tag），如果 SPS/PPS 嵌在每帧数据里而不在 `extradata` 中，FLV 头部就缺少视频解码所必需的信息。

**本质**：SPS/PPS 放错了位置——应该放在**容器头部**（`extradata`）供全局使用，而不是**分散在每帧数据**中。

### 3.4 面试加分项：主动抛出"逆向场景"

> 对于裸 `.h264` 文件，恰好**不能**设 `GLOBAL_HEADER`。
>
> 因为裸流没有容器头部，播放器只能从帧数据中读取 SPS/PPS。如果也设了该标志，SPS/PPS 被放入 `extradata` 但裸流文件不写 `extradata`，播放器就无法解码。
>
> 同一标志，FLV/RTMP 推流**必须设**，裸 .h264 文件**不能设**——取决于封装格式是否有"全局头部"这个容器。

| 场景 | 是否需要 `GLOBAL_HEADER` | 原因 |
|------|:---:|------|
| FLV / RTMP 推流 | ✅ 必须设 | `avformat_write_header()` 从 `extradata` 读 SPS/PPS 写入 FLV header |
| MP4 本地录制 | ✅ 建议设 | MP4 的 `moov box` 也需要从 `extradata` 获取 codec 配置 |
| 裸 .h264 文件 | ❌ 不能设 | 没有容器头部，播放器只能从帧数据中读 SPS/PPS |

### 3.5 面试官可能追问的方向

| 追问 | 答题方向 |
|------|----------|
| "报的是什么错？" | 客户端：`WSAECONNRESET`（连接被对端重置）；服务端 Nginx 日志：`"codec: invalid video codec header size=5"` |
| "除了设置 GLOBAL_HEADER，还有其他需要保证的吗？" | 1) `avformat_write_header` 之后 `time_base` 已正确设置；2) 音视频 pts/dts 单调递增 |
| "如果是网络层面的断连怎么办？" | 需要断线重连 + 指数退避策略（项目中 `ExponentialReconnectPolicy` 已定义接口，是预留扩展点） |
| "怎么调试定位到这个问题的？" | 客户端报错信息不足 → 查看 Nginx 服务端日志 → 搜索 Google → 确认是 SPS/PPS header 问题 → 加 `GLOBAL_HEADER` |

---

## 四、用户回答评估

| 评估维度 | 得分 | 点评 |
|----------|:----:|------|
| 调试流程 | ✅ 优秀 | 完整讲述了"客户端报错 → 服务端日志 → Google 搜索 → 找到根因"的排查路径，面试官通常喜欢听踩坑故事 |
| 解决方案 | ✅ 正确 | `flags \|= AV_CODEC_FLAG_GLOBAL_HEADER` 是正确修复方式 |
| 原理理解 | ⚠️ 方向对但不够精准 | "Nginx 只看初始元数据"的表述方向正确，但建议补充因果链中缺失的环节：**SPS/PPS 没进 `extradata` → `avformat_write_header` 写不出 FLV header → Nginx 解析失败** |
| 深度拓展 | ⚠️ 未展开 | 没有提到"裸 .h264 不能设 GLOBAL_HEADER"的逆向场景，这是一个很好的加分机会 |

### 改进建议

1. **原理表述升级**：将"Nginx 只看初始元数据"补充为完整的因果链——`GLOBAL_HEADER` → SPS/PPS 进 `extradata` → `avformat_write_header` 写入 FLV header → Nginx 解析成功。
2. **主动抛出逆向场景**：面试时补充"裸 .h264 恰好相反，不能设 `GLOBAL_HEADER`"，展示对标志在不同封装格式下行为的全面理解。

---

## 五、关键要点速记

| 要点 | 内容 |
|------|------|
| **报错位置** | 客户端：`av_interleaved_write_frame`（WSAECONNRESET）<br>服务端：`"codec: invalid video codec header size=5"` |
| **根因一句话** | `extradata` 为空 → `avformat_write_header` 写 FLV header 时缺少 SPS/PPS |
| **解决方案** | `m_ctx->flags \|= AV_CODEC_FLAG_GLOBAL_HEADER` |
| **标志作用** | 将 SPS/PPS 从"每帧数据"迁移到编码器的 `extradata` 字段 |
| **FLV 必须设** | RTMP/FLV 推流需要 SPS/PPS 在容器头部（`extradata`），由 `avformat_write_header` 写入 |
| **裸 .h264 不能设** | 裸流无容器，播放器从帧数据读取 SPS/PPS，设了反而丢失 |
| **排查思路** | 客户端报连接重置 → 查服务端日志 → 搜错误信息 → 定位 header 问题 |
| **关联知识点** | 断线重连（指数退避）、`av_compare_ts` 交错策略、`avio_open2` 网络 IO |

---

## 六、一句话总结

> **`AV_CODEC_FLAG_GLOBAL_HEADER` 控制 SPS/PPS 的存放位置——FLV/RTMP 推流必须将其放入 `extradata` 供 `avformat_write_header` 写入容器头部，否则 Nginx-RTMP 解析不到视频解码信息，报 `"invalid video codec header"` 并断开连接。**
