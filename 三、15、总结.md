# 三、15、总结 — 音频 FIFO 队列：为什么用 AVAudioFifo 而不是 BlockingQueue？

---

## 一、面试问题

> **为什么要使用 `AVAudioFifo`（`AudioFifoQueue`）？直接用 `BlockingQueue<AVFrame*>` 不行吗？**

---

## 二、用户原始回答

### （1）音频帧的采样点问题

> 在该项目中，进行音频编码时，编码器接收的是 `AVFrame` 也就是所谓的一帧 PCM 音频原始数据包，而不同于视频帧处理时，并没有音频帧这个概念。一个音频包中，并没有规定要有多少个采样点才合适，但是音频编码器对于接收的音频包的采样点数有一些规定，比如 `libmp3lame` 编码器要求传入的音频包需要有 1152 采样点数，其余一般的编码器要求为 1024 个采样点数。为了方便编码，所以用户需要自行处理音频原始数据包，保证最终给到编码器的数据包含有编码器期望的采样点数。

### （2）AVAudioFifo 的作用

> 如果用户手动实现这样的一个特殊的队列，是很麻烦的，但是 FFmpeg 提供了拥有输出一帧 `AVFrame`，保证其有指定采样点数的这样一个 `AVAudioFifo`，音频共享队列。用户只需要将重采样后的音频数据给到其内部，那么消费端在取出的时候，就会自己处理好这一切。

### （3）为什么 BlockingQueue 不行

> 从以上的表述中来看，直接使用 `BlockingQueue<AVFrame*>` 显然是不可行的，因为它输出的 `AVFrame` 内部的采样点数不一定符合编码器的期望，因为其内部仅仅只是一个队列而已，没有解决这个问题的能力。

---

## 三、标准参考回答

> `BlockingQueue` 是**帧级别**的 FIFO，但音频编码器要求的输入 `AVFrame` 必须有**固定数量**的采样点（AAC 通常 1024）。原始帧采样点数不固定（可能是 1002、1044 等）。`AVAudioFifo` 提供**采样点级别**的 FIFO：
> - **Push**：把整帧采样数据拷入内部环形缓冲区
> - **Pop**：精确取出 `nb_samples` 个采样点拼成新 AVFrame
> - 如果队列中不足 `nb_samples` 个采样点，消费者**阻塞等待**
>
> 这是普通 `BlockingQueue<AVFrame*>` 做不到的。

---

## 四、评审意见与改进建议

### 4.1 用户回答的优势

| 方面 | 说明 |
|------|------|
| 核心逻辑 | 三部分回答整体方向正确，抓住了"采样点对齐"这一核心矛盾 |
| 编码器参数 | MP3=1152、AAC=1024 的数值准确 |
| 结论 | `BlockingQueue` 无法解决采样点对齐问题，结论正确 |

### 4.2 存在问题与改进

| 用户原表述 | 问题所在 | 建议改为 |
|---|---|---|
| "并没有音频帧这个概念" | 在 FFmpeg 中 `AVFrame` 既可承载视频也可承载音频，**音频帧的概念是存在的**（`nb_samples` 即采样点数） | "音频采集解码得到的 `AVFrame`，其 `nb_samples` **不是固定的**——每次可能得到 1002、1044 等不同数量" |
| "音频共享队列" | 翻译不标准，"共享"容易误解为多线程共享 | "**采样点级别的音频 FIFO 缓冲区**（`AVAudioFifo`）" |
| "仅仅只是一个队列而已" | 表述过于口语化，缺乏深度 | "帧级 FIFO，入队出队操作的都是完整 `AVFrame*`，**不涉及采样点的拆分与重组**，无法对齐编码器要求" |

### 4.3 核心一句话总结（面试金句）

> **`AVAudioFifo` 和 `BlockingQueue` 的本质区别在于粒度：**
> - 前者以**采样点**为操作单元，能够"化零为整"拼出编码器需要的固定采样点帧；
> - 后者以**完整 AVFrame** 为单元，只是原样传递，无法解决采样点对齐问题。

### 4.4 底层原理加深理解

| 对比维度 | `BlockingQueue<AVFrame*>` | `AVAudioFifo`（采样点级 FIFO） |
|---|---|---|
| **操作粒度** | 帧级（整个 `AVFrame`） | 采样点级（逐个 sample） |
| **入队行为** | 直接存储 `AVFrame*` 指针，不做数据拆分 | 拆解 `AVFrame`，按采样点粒度拷贝到内部环形缓冲区 |
| **出队行为** | 原样返回入队时的 `AVFrame*` | 从缓冲区头部按编码器要求的 `nb_samples`（如 1024）精确取出并**重新组装**成新 `AVFrame` |
| **缓冲区不足时** | 返回 `false` 或阻塞 | 阻塞等待，直到缓冲区累积足够采样点（通过 `m_cvNotEmpty` 条件变量实现） |
| **适用场景** | 视频帧传递（每帧天然完整） | 音频帧传递（需要采样点级对齐编码器要求） |
