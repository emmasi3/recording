# 六、31、总结 — RTMP 推流为什么选 FLV 封装格式

---

## 一、问题

> **31. RTMP 推流用的是什么封装格式？为什么选 FLV？**

---

## 二、用户原始回答

用户回答包含两个要点：

1. RTMP 推流用的 **FLV** 格式。首先，RTMP 协议**原生支持 FLV 格式**，Nginx-RTMP、SRS 等主流流媒体服务器原生支持，**兼容性最好**。
2. FLV **格式简单**，没有像其他格式（如 MP4）那样的**头信息限制**。

---

## 三、标准参考回答

### 3.1 精简版（面试核心三句话）

| 序号 | 要点 | 具体说明 |
|:--:|------|----------|
| 1 | **协议原生** | RTMP 协议就是为传输 FLV 设计的，Nginx-RTMP、SRS 等对 FLV over RTMP 支持最完善、bug 最少 |
| 2 | **流式友好** | FLV 采用 Tag 级别增量结构，支持**边收边写**，不像 MP4 那样必须最后才写 `moov box` 再回头修正文件头 |
| 3 | **格式简单** | FLV 的 Tag 解析逻辑比 MP4 的 Atom/Box 体系简单得多，推流端和服务端 CPU 开销更低 |

代码体现：
```cpp
avformat_alloc_output_context2(&m_ctx, nullptr, "flv", m_url.c_str());
```

---

### 3.2 详细版（主动加分 / 应对追问）

#### （一）核心原因

**FLV 是"流式友好"格式，MP4 是"事后修补"格式。**

一条 FLV 流的结构如下：

```
FLV Header → Script Tag(metadata) → Video Tag → Audio Tag → Video Tag → ...
```

- 先写 `avformat_write_header()` → 输出 FLV Header + Script Tag（含 onMetaData、SPS/PPS extradata）
- 之后每来一帧，直接追加一个 video tag 或 audio tag，**永远不需要回头修改**

MP4 的致命问题：
- `moov atom/box` 包含时长、帧索引等元数据，**通常在文件末尾写入**
- 录制完才能确定确切信息，再回头 "fix up" 文件头
- **推流是实时、持续的过程，没有"写完最后再回头改开头"的可能**

#### （二）FLV Tag 结构 vs MP4 Atom 结构对比

| 维度 | FLV | MP4 |
|------|-----|-----|
| 基本单元 | Tag（自包含、独立可写） | Atom/Box（树状嵌套结构） |
| 元数据位置 | `avformat_write_header()` 时直接写入 | `moov box` 通常在文件末尾 |
| 写入方式 | 增量追加，无需 seek | 需要写完末尾再回头 seek 修正 |
| seek 支持 | 差（无索引结构） | 好（`moov` 含有 sample table） |
| 典型场景 | 直播 / 推流 / 监控 | 点播 / 本地录制 / 文件分发 |

#### （三）面试官可能追问的延伸问题

| 追问 | 答题方向 |
|------|----------|
| "FLV 和 MP4 还有什么区别？" | FLV：Tag 结构、无 seek 索引、适合直播；MP4：Atom/Box 结构、有 `moov`/`moof` 索引、适合点播 |
| "为什么不用 TS？" | TS 也可以直播推流（HLS），但 RTMP 协议不支持 TS，TS 主要用于 HLS over HTTP |
| "你的项目中 FLV header 是什么时候写的？" | `avformat_write_header()` → 输出 FLV header + metadata tag（前提：设了 `AV_CODEC_FLAG_GLOBAL_HEADER`，确保 SPS/PPS 写入 extradata） |
| "如果要求本地录制也用 MP4 怎么办？" | 可以用 fragmented MP4（fMP4 / `-movflags frag_keyframe`）或 `-flvflags no_duration_filesize` 方案，但实现复杂度更高 |

#### （四）项目中与此相关的踩坑经验

设置 `AV_CODEC_FLAG_GLOBAL_HEADER` 是本问的关键关联知识点（见第 21 问 / 第 33 问）：

> 不设该 flag → SPS/PPS 不会写入 FLV header 的 extradata → 推流时 Nginx 报 `"codec: invalid video codec header size=5"` → RTMP 连接中断。

这段实战经验可以在回答第 31 问时主动抛出，展示自己不仅知道"选 FLV"，还知道 FLV header 里具体需要放什么、以及没放对会导致什么后果。

---

## 四、用户回答评估

| 评估维度 | 得分 | 点评 |
|----------|:----:|------|
| 核心方向 | ✅ 正确 | 两个要点（协议原生 / 格式简单）都命中核心 |
| 技术深度 | ⚠️ 偏浅 | 第 (2) 点"头信息限制"表述不够精准，建议具体化为"MP4 需要 moov box 事后修补，FLV 支持增量写入" |
| 可扩展性 | ⚠️ 一般 | 可以主动关联 `AV_CODEC_FLAG_GLOBAL_HEADER` 的实战经验作为加分项 |

**改进建议**：将第 (2) 点从"flv 格式简单"升级为"FLV 的 Tag 结构支持增量写入、实时推流，而 MP4 的 moov box 需要写完文件后再回头修正文件头，对推流场景是致命缺陷"。
