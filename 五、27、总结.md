# 面试题 27：MuxThreadProc 中 `av_compare_ts` 与阻塞逻辑

---

## 一、原题

> **27. MuxThreadProc 中 `av_compare_ts` 是干什么的？它怎么决定"这次编码视频还是音频"？**

---

## 二、用户原始回答

> **（1）av_compare_ts 方法**
>
> `av_compare_ts` 方法用来比较两个在各自时间基上的数值的大小。在该项目中，用于音视频同步逻辑：
> - 如果 `<= 0`，说明**视频落后**，立即去编码视频
> - 如果 `> 0`，则去编码音频
>
> **（2）阻塞逻辑**
>
> 在进入某一个读取并编码的分支后，采用的是阻塞的逻辑。如果有意外情况发生，编码线程会直接阻塞，生产者可能会填满整个缓冲区，但是并不会满溢，直到消费者开始恢复为止。

---

## 三、标准参考回答

### 3.1 `av_compare_ts` 详解

`av_compare_ts(m_last_video_pts, v_tb, m_last_audio_pts, a_tb)` 会将视频 PTS 和音频 PTS **分别按各自时间基 `v_tb` / `a_tb` 换算到同一时间基下**比较大小：

| 返回值 | 含义 | 动作 |
|--------|------|------|
| `<= 0` | 视频 PTS 早于或等于音频 PTS，视频落后 | 编码视频追上来 |
| `> 0` | 音频落后 | 编码音频追上来 |

> 这是一种**以时间戳追赶为核心的简易 A/V 同步策略**——谁的时间戳滞后就优先编码谁，不断交替拉齐音视频进度。

### 3.2 阻塞逻辑详解

整个混流线程采用**生产者-消费者模型**，存在两种不同层级的"阻塞"：

#### 3.2.1 正常情况：队列为空时的阻塞

```cpp
// 视频分支 (cmp <= 0):
dxgiCap->ReadFrame()   // → WaitAndPop() 阻塞等待视频帧

// 音频分支 (cmp > 0):
audioCap->ReadFrame()  // → WaitAndPop() 阻塞等待音频帧
```

`WaitAndPop` 使用 `std::condition_variable::wait()`，当队列为空时消费者线程挂起，**不空转消耗 CPU**，等待生产者 `notify_one` 唤醒。

#### 3.2.2 异常情况：编码/写包环节的阻塞

当进入某个分支后，流程为：

```
ReadFrame() → Encode() → WritePacket()
```

`WritePacket` 可能因以下原因阻塞：
- **本地录制**：磁盘 IO 慢
- **RTMP 推流**：网络拥塞、带宽不足

此时消费者整体停顿，生产者持续投喂数据到队列中。

#### 3.2.3 无界队列的风险

当前队列实现使用 `std::queue`（无容量上限）：

| 表现 | 分析 |
|------|------|
| 不会丢帧 | Push 永远成功，数据不丢失 |
| 不会满溢 | 队列无上限，不会拒绝写入 |
| 内存风险 | 消费者长时间阻塞时，队列无限增长，可能导致 OOM |

**改进方案**：设置环形缓冲区 + 容量上限，满时丢弃最老帧，保证内存可控。

### 3.3 补充：极端边界场景

代码中存在一个被注释掉的 `sleep_for(1ms)`（约第 502 行），说明作者也考虑过**两个队列都为空但 `done = false`** 时的 CPU 空转问题——主循环会反复检查两个空队列，形成忙等。加入微休眠是常见的缓解手段。

---

## 四、回答对比与改进建议

| 维度 | 用户回答 | 标准回答 | 差距分析 |
|------|----------|----------|----------|
| `av_compare_ts` 理解 | 基本正确，知道"谁落后喂谁" | 补充了"转换到公共时间基再比较"的底层原理 | 缺少内部机制说明 |
| 阻塞逻辑 - 正常场景 | 提到了阻塞 | 明确区分了 `WaitAndPop` 阻塞（队列空）和 `WritePacket` 阻塞（IO/网络） | 混淆了两种不同阻塞 |
| 阻塞逻辑 - 异常场景 | "填满缓冲区但不溢出" | 点出无界队列的 OOM 风险及改进方案 | 未识别隐患 |
| 边界条件 | 未提及 | 提到了 busy-wait 和 `sleep_for` 注释 | 遗漏 |

### 面试官可能追问的点

1. **"无界队列有什么风险？你怎么改进？"** → 回答：环形缓冲区 + 容量限制 + 丢最老帧策略
2. **"消费者一直阻塞在 WritePacket，生产者的 Push 会永不返回吗？"** → 不会，Push 是纯内存操作（入队 + notify），不受 IO 影响
3. **"这个同步策略能保证音视频严格对齐吗？"** → 不能，只是粗粒度"追赶"策略。严格同步需要引入外部时钟（如 `sync` 参数）做 PTS 伸缩和丢帧决策

---

## 五、关键代码索引

- `av_compare_ts` 调用处：`src/adapters/output/FfmpegMuxerStreamer.cpp` ~第 460 行
- `WaitAndPop` 实现：`include/concurrency/ConcurrentQueue.h` ~第 130 行
- `Push` 实现：`include/concurrency/ConcurrentQueue.h` ~第 98 行
