# 二、11、BGRA → NV12 转换方案总结

---

## 一、问题

**BGRA → NV12 转换你是怎么做的？为什么不用 FFmpeg 的 `sws_scale`？**

---

## 二、用户原始回答

> （1）利用 DXGI 获取到的桌面帧纹理，储存在 GPU 显存中，通过 D3D11 提供的 **VideoProcessor** 工具进行纹理的重采样 && 格式转换；
>
> （2）如果使用 FFmpeg 的 `sws_scale` 无疑是非常方便的，但是需要通过 D3D11 提供的 `Map()` 接口，将 GPU 显存中 CPU 不可访问的纹理资源通过 CPU 拷贝到 CPU 可访问的内存中，这是非常耗时的，而该项目涉及到 RTMP 实时推流，所以对于每一帧都拷贝，性能瓶颈明显，并且在重采样之后，还是要通过 CPU 拷贝到 GPU 侧进行硬编码，涉及到两次拷贝；
>
> 而如果直接利用 D3D11 提供的视频处理接口，在 GPU 侧进行处理，处理后直接送去硬编码，全程 0 拷贝，性能提升非常明显。

---

## 三、标准参考回答

### （一）方案选择：D3D11 VideoProcessor（`VideoProcessorBlt`）

利用 DXGI 获取到的桌面帧纹理（`DXGI_FORMAT_B8G8R8A8_UNORM`）存储在 GPU 显存中，属于 `D3D11_USAGE_DEFAULT` 资源，**CPU 无法直接访问**。

通过 D3D11 提供的 **VideoProcessor** 接口（核心 API：`VideoProcessorBlt`）在 GPU 内部完成三项转换：

| 转换类型 | 输入 | 输出 |
|---------|------|------|
| **像素格式转换** | BGRA（`B8G8R8A8_UNORM`） | NV12 |
| **颜色空间转换** | RGB | YUV（BT.601 / BT.709） |
| **色彩范围映射** | Full Range（0-255） | Limited Range（16-235） |

转换结果直接写入 `av_hwframe_get_buffer` 分配的硬件帧纹理中，然后直接送入 NVENC 硬件编码器。

### （二）为什么不用 FFmpeg 的 `sws_scale`？

如果用 `sws_scale` 做 CPU 侧转换，完整流程为：

```text
步骤①：av_hwframe_transfer_data  —→  将 GPU 纹理下载到 CPU 内存（PCIe 写入）
步骤②：sws_scale                 —→  在 CPU 上完成 BGRA → NV12 格式转换
步骤③：av_hwframe_transfer_data  —→  将转换结果上传回 GPU 显存（PCIe 读回）
```

**核心问题：两次 PCIe 传输 + CPU 参与转换计算**

#### 量化性能分析（1080p@60fps 场景）

| 对比维度 | `sws_scale`（CPU 路径） | `VideoProcessorBlt`（GPU 路径） |
|---------|------------------------|-------------------------------|
| **PCIe 带宽（BGRA 下传）** | ~497 MB/s | **0** |
| **PCIe 带宽（NV12 上传）** | ~187 MB/s | **0** |
| **CPU 转换耗时** | 每帧 3~5ms | **0**（GPU Video Engine 独立处理） |
| **总 PCIe 带宽** | ~684 MB/s | **0** |
| **延迟** | 高（等待两次传输 + CPU 计算） | **极低**（GPU 内部直通） |
| **对 CPU 的影响** | 占用 CPU 核心做像素计算 | **0% CPU** |

对于 1080p@60fps 的实时 RTMP 推流场景，`sws_scale` 方案几乎是不可接受的性能瓶颈；而 `VideoProcessorBlt` 方案做到**全程 GPU 内部零拷贝、零 PCIe 传输、零 CPU 参与**。

---

## 四、用户回答点评与改进点

### ✅ 回答优点

1. **核心矛盾抓得准**：GPU 零拷贝 vs. CPU 中转的两趟 PCIe 开销
2. **技术方案对**：D3D11 VideoProcessor 在 GPU 内部完成转换
3. **结合业务场景**：RTMP 实时推流，性能敏感，必须避免不必要的内存拷贝

### ⚠️ 需改进的细节

| 问题 | 用户表述 | 应修正为 |
|------|---------|---------|
| **术语错误** | "Video**Progress**" | 正确名称为 **VideoProcessor**，核心 API 是 `VideoProcessorBlt` |
| **流程描述不精确** | "通过 `Map()` 接口将 GPU 纹理拷贝到 CPU" | DXGI 桌面纹理是 `D3D11_USAGE_DEFAULT`，CPU **不能直接 Map**；实际 CPU 路径需要 `CopyResource` → Staging 纹理 → `Map` → `sws_scale` → 上传；或使用 `av_hwframe_transfer_data` 一步完成 GPU↔CPU 传输 |
| **"重采样"表述** | "纹理的重采样 && 格式转换" | 在音视频领域，"重采样"（resampling）通常指**音频采样率转换**，这里应表述为 **"格式转换（BGRA→NV12）+ 颜色空间转换（RGB→YUV）+ 色彩范围映射（Full→Limited Range）"** |

### 📝 优化后的标准话术

面试时建议按以下逻辑回答：

> **方案选择**：使用 D3D11 VideoProcessor（`VideoProcessorBlt`）在 GPU 内部完成 BGRA → NV12 的**格式转换**、RGB → YUV 的**颜色空间转换**，以及 Full Range（0-255）→ Limited Range（16-235）的**色彩范围映射**，转换结果直接写入 `av_hwframe_get_buffer` 分配的硬件帧纹理中。
>
> **为何不用 sws_scale**：如果用 FFmpeg 的 `sws_scale`，流程是：`av_hwframe_transfer_data` 下载 GPU 纹理到 CPU → `sws_scale` 在 CPU 做 BGRA→NV12 转换 → `av_hwframe_transfer_data` 上传回 GPU 交给 NVENC。这涉及**两次 PCIe 传输**——1080p@60fps 下仅 BGRA 下载就约 500MB/s 的带宽开销，加上 `sws_scale` 本身的 CPU 时间（每帧 3~5ms），延迟和 CPU 占用都非常可观。
>
> 而 VideoProcessor 方案全程在 GPU 内部完成，处理后**直接送去 NVENC 硬编码器**，真正做到 **0 次 CPU 拷贝、0 次 PCIe 传输、0% CPU 参与转换计算**——这就是为什么选择 D3D11 VideoProcessor 而不是 `sws_scale` 的根本原因。

---

## 五、核心代码对应

转换逻辑位于 `src/adapters/capture/DxgiScreenCapture.cpp` 的 `ConvertDesktopBGRA_To_AVFrameNV12()` 函数：

```
srcBGRA (DXGI AcquireNextFrame 返回的桌面纹理)
    │
    ▼
CreateVideoProcessorInputView(srcBGRA, ...)     // BGRA 输入视图
    │
    ▼
CreateVideoProcessorOutputView(dstTex, ...)      // NV12 输出视图
    │
    ▼
VideoProcessorBlt(vp, outView, 0, 1, &stream)   // GPU 内部完成全部转换
    │
    ▼
dstTex → NVENC 编码器（avcodec_send_frame）
```

**关键颜色空间参数设置：**

| 参数 | 值 | 含义 |
|------|---|------|
| `inCS.RGB_Range` | `1` | 输入为 Full Range RGB（0-255），对应桌面 BGRA 纹理 |
| `outCS.Nominal_Range` | `D3D11_VIDEO_PROCESSOR_NOMINAL_RANGE_16_235` | 输出为 Limited Range YUV，符合视频编码行业标准 |

> 如果范围映射不正确（比如输出也设 Full Range），编码后的画面会过曝或过暗。

---

## 六、知识点速记

| 关键词 | 说明 |
|--------|------|
| **VideoProcessorBlt** | D3D11 提供的 GPU 视频处理接口，可一次性完成格式/色域/范围转换 |
| **零拷贝** | 整个 BGRA→NV12→NVENC 链路数据始终留在 GPU 显存中 |
| **两次 PCIe 传输** | `sws_scale` 方案需要 GPU→CPU（下载）+ CPU→GPU（上传） |
| **Full → Limited Range** | 桌面 BGRA 是 0-255，NVENC 期望 16-235，VideoProcessor 自动映射 |
| **`D3D11_USAGE_DEFAULT`** | DXGI 桌面纹理的使用类型，CPU 不可直接 Map |
| **`av_hwframe_transfer_data`** | FFmpeg 的 GPU↔CPU 数据传输 API，每次调用就是一次 PCIe 传输 |

---

> **一句话总结**：用 GPU 的 VideoProcessorBlt 替代 CPU 的 sws_scale，把 BGRA→NV12 的格式转换锁死在 GPU 内部完成，从而在整个采集→转换→编码链路中实现真正的零拷贝。
