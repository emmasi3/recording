# 四、24 — PTS 计算方式总结

---

## 一、面试问题

> **PTS 计算方式：视频和音频分别怎么算？编码前后的时间戳缩放做了什么？**

---

## 二、用户原始回答

### （1）视频 PTS

> 通过 ffmpeg 提供的时间基转换函数，将编码包索引（也就是送入的编码包数量），帧率倒数为时间基，目标时间基为设定好的编码器上下文的时间基，进行转换，即为视频的 PTS。

### （2）音频 PTS

> 由于音频编码器给出的音频 AVPacket 包，采样点数量是固定的，所以用该数值作为一帧的标准，在给到编码器之前，通过采样点的数量进行 PTS 计算，开始为 0，此时时间基为采样率的倒数。

### （3）编码后

> 通过 `av_packet_rescale_ts` 对视频和音频编码包的时间有关字段统一缩放到目标容器格式期望的时间基准上。

---

## 三、标准参考回答

### 3.1 视频帧 PTS

```
pts = av_rescale_q(m_encode_frame_index++, {1, m_ctx->framerate.num}, m_ctx->time_base)
```

- **源时间基**：帧率的倒数 `{1, framerate.num}`，如 60fps → `{1, 60}`
- **目标时间基**：编码器上下文的 `m_ctx->time_base`
- **转换方式**：用 `av_rescale_q` 把帧序号从帧率时间基换算到编码器时间基
- **赋值时机**：**编码前**设置到 `frame->pts`，然后送入编码器

> **关键纠正**：用户提到的"编码包索引"应为 **"帧序号"（frame index）**，因为这个计数发生在 `avcodec_send_frame` 之前，是对原始帧（AVFrame）的计序，而非对编码包（AVPacket）的计数。

### 3.2 音频帧 PTS

```
pts = m_nbSamples * m_encode_frame_index++
```

- **源时间基**：采样率的倒数 `{1, sample_rate}`（如 44100Hz → `{1, 44100}`）
- **计算方式**：PTS = 每帧采样点数 × 帧序号，因为时间基就是采样率倒数，直接用累计采样点数即可
- **采样点数来源**：`m_ctx->frame_size`（编码器要求的每帧输入采样点数，如 AAC = 1024）

> **关键纠正**：用户提到的"音频编码器给出的 AVPacket 包采样点数固定"是**概念混淆**。实际上：
> - **AVFrame（编码前）**：采样点数由编码器 `frame_size` 决定，是**编码器输入要求**，不是输出包的性质
> - **AVPacket（编码后）**：是压缩后的 AAC 数据，采样点数与输入帧的对应关系由编码器内部处理

### 3.3 编码后时间基缩放

```
av_packet_rescale_ts(m_pkt, m_ctx->time_base, m_out_time_base)
```

- **作用**：将 AVPacket 的 `pts`、`dts`、`duration` 三个字段统一从编码器时间基缩放到输出流时间基
- **视频**：从 `{1, 60}`（或编码器细分时间基）→ `m_out_time_base`（通常 `{1, 1000}`）
- **音频**：从 `{1, 44100}` → `m_out_time_base`
- **必要性**：编码器和容器格式的时间基准粒度不同，必须统一，否则播放器无法正确解析时间戳

---

## 四、用户回答 vs 标准回答对照表

| 分项 | 用户回答 | 标准回答 | 评估 |
|------|---------|---------|------|
| 视频 PTS 源时间基 | 帧率倒数 ✅ | `{1, framerate.num}` | ✅ 正确 |
| 视频 PTS 换算函数 | `av_rescale_q` ✅ | `av_rescale_q(index, src_tb, dst_tb)` | ✅ 正确 |
| 视频 PTS 术语 | "编码包索引" ❌ | "帧序号"（AVFrame 计数） | ❌ **混淆了 AVFrame 和 AVPacket** |
| 音频固定采样点来源 | "AVPacket 包" ❌ | 编码器 `frame_size`（输入要求） | ❌ **因果倒置，关键错误** |
| 音频 PTS 计算公式 | nbSamples × index ✅ | `m_nbSamples * m_encode_frame_index++` | ✅ 正确 |
| 音频 PTS 时间基 | 采样率倒数 ✅ | `{1, sample_rate}` | ✅ 正确 |
| 编码后缩放函数 | `av_packet_rescale_ts` ✅ | 统一缩放 pts/dts/duration | ✅ 正确 |
| 编码后缩放目标 | 容器格式时间基 ✅ | `m_out_time_base` | ✅ 正确 |
| 两层时间基转换原因 | 未提及 🔶 | 编码器细粒度 → 容器标准粒度 | 🔶 可补充 |

---

## 五、核心概念澄清

### 5.1 AVFrame vs AVPacket 区别

| | AVFrame（原始帧） | AVPacket（编码包） |
|---|---|---|
| **阶段** | 编码前 / 解码后 | 编码后 / 解码前 |
| **数据形式** | 原始像素 / PCM 采样 | H.264 / AAC 压缩数据 |
| **PTS 设置时机** | `avcodec_send_frame` 之前手动设 | 编码器继承 frame->pts 后，再缩放 |
| **视频"单位"** | 一帧画面 | 一个 H.264 NALU 包 |
| **音频"单位"** | 固定采样点数（如 1024） | 一个 AAC 压缩包 |

### 5.2 两层时间基转换的完整链路

```
采集帧 → 编码前 PTS（编码器时间基）
           ↓ avcodec_send_frame / avcodec_receive_packet
         编码包（编码器时间基）
           ↓ av_packet_rescale_ts
         编码包（输出流时间基）
           ↓ av_interleaved_write_frame
         写入文件 / 推流
```

### 5.3 具体计算示例

**视频（60fps，编码器 time_base = {1, 60}，输出 time_base = {1, 1000}）：**

| 帧序号 | 编码前 pts | 编码后 pts（换算） |
|--------|-----------|-------------------|
| 0 | `av_rescale_q(0, {1,60}, {1,60})` = 0 | `av_rescale_q(0, {1,60}, {1,1000})` = 0ms |
| 1 | `av_rescale_q(1, {1,60}, {1,60})` = 1 | `av_rescale_q(1, {1,60}, {1,1000})` ≈ 16.67ms |
| 30 | 30 | 500ms |
| 60 | 60 | 1000ms (1s) |

**音频（44100Hz，frame_size=1024，输出 time_base = {1, 1000}）：**

| 帧序号 | 编码前 pts（采样点数） | 编码后 pts（换算） |
|--------|---------------------|-------------------|
| 0 | 0 × 1024 = 0 | 0ms |
| 1 | 1 × 1024 = 1024 | 1024/44100 ≈ 23.2ms |
| 43 | 43 × 1024 = 44032 | 44032/44100 ≈ 998.5ms |

---

## 六、改进后的完整回答（建议背诵版本）

面试时间答时可以这样说：

> **视频 PTS**：编码前，用 `av_rescale_q` 把**帧序号**从帧率倒数的时间基（比如 60fps 就是 `{1, 60}`）换算到编码器上下文的时间基 `m_ctx->time_base`，然后赋值给 `frame->pts`，再送入编码器。注意这里计数器 `m_encode_frame_index` 递增的对象是原始帧（AVFrame），不是编码包。
>
> **音频 PTS**：AAC 编码器要求每帧输入的 **AVFrame 必须有固定数量的采样点**（`m_ctx->frame_size`，通常是 1024），这是编码器输入的要求。AudioFifoQueue 从采样点级 FIFO 中精确取出 1024 个采样点拼成完整 AVFrame，编码前直接用 `pts = nbSamples × frame_index` 计算，因为音频编码器的时间基就是采样率的倒数 `{1, sample_rate}`，采样点累计数天然对应时间。
>
> **编码后**：两路编码包出来之后，都通过 `av_packet_rescale_ts(pkt, ctx->time_base, out_time_base)` 把 pts / dts / duration 从编码器自己的时间基统一缩放到输出流的目标时间基（FLV 容器通常是 `{1, 1000}`）。两层转换必不可少，因为编码器用细粒度时间基保证精度，容器格式用标准时间基保证兼容性。

---

## 七、常见追问及应对

| 追问 | 应对 |
|------|------|
| "为什么视频不用帧序号直接当 PTS？" | 因为编码器 time_base 可能不是 `{1, fps}`，比如设置为 `{1, 90000}`，需要正确换算 |
| "音频为什么不用 `av_rescale_q`？" | 因为音频 time_base = `{1, sample_rate}`，PTS = nbSamples × index 已经是正确值，等价于 `av_rescale_q(nbSamples×index, {1,sample_rate}, {1,sample_rate})` |
| "`av_packet_rescale_ts` 改了哪些字段？" | pts、dts、duration，三个字段一起缩放 |
| "如果输出流是 MP4，time_base 是什么？" | MP4 通常是 `{1, 90000}`，比 FLV 的 `{1, 1000}` 精度高 90 倍 |
